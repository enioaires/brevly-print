---
phase: 01-foundation-thread-model-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - src/lib.rs
  - src/app_dir.rs
  - src/config_store.rs
  - src/credential_store.rs
  - tests/config_store_test.rs
  - tests/credential_store_test.rs
  - .github/workflows/ci.yml
autonomous: true
requirements: []   # Phase 1 is a pure enabling spike — no v1 REQUIREMENTS IDs (see ROADMAP §Phase 1). Goal-backward from SC-2/SC-4.
user_setup: []

must_haves:
  truths:
    - "The full v1 dependency set compiles for the Windows target (SC-4)"
    - "%APPDATA%\\BrevlyPrint\\ is created idempotently on first run (SC-2)"
    - "cargo test runs the Wave 0 test suite on the Windows/GHA runner without missing-file errors"
    - "GitHub Actions Windows CI builds release + runs tests on every push"
  artifacts:
    - path: "Cargo.toml"
      provides: "Full v1 dependency set at RESEARCH.md-verified versions; lib+bin split targets"
      contains: "winit"
    - path: "src/lib.rs"
      provides: "Library crate root re-exporting init_app_dir, ConfigStore, CredentialStore"
      contains: "pub mod app_dir"
    - path: "src/app_dir.rs"
      provides: "init_app_dir() -> PathBuf (%APPDATA%\\BrevlyPrint\\) with idempotency test"
      contains: "pub fn init_app_dir"
    - path: "src/config_store.rs"
      provides: "ConfigStore skeleton + MIGRATIONS constant stub the Wave 2 plan fills in"
      contains: "pub struct ConfigStore"
    - path: "src/credential_store.rs"
      provides: "CredentialError enum + CredentialStore skeleton the Wave 2 plan fills in"
      contains: "pub enum CredentialError"
    - path: "tests/config_store_test.rs"
      provides: "Wave 0 SQLite integration test scaffold (schema v1, config write/read, user_version)"
      contains: "config_store"
    - path: "tests/credential_store_test.rs"
      provides: "Wave 0 DPAPI integration test scaffold (round-trip, missing, corrupt)"
      contains: "credential_store"
    - path: ".github/workflows/ci.yml"
      provides: "Windows-runner build+test gate (WGPU_BACKEND=dx12)"
      contains: "windows-latest"
  key_links:
    - from: "src/lib.rs"
      to: "src/app_dir.rs, src/config_store.rs, src/credential_store.rs"
      via: "pub mod declarations + re-exports"
      pattern: "pub (mod|use)"
    - from: "tests/config_store_test.rs"
      to: "brevly_print::config_store / brevly_print::init_app_dir"
      via: "extern crate import of the lib crate"
      pattern: "brevly_print::"
    - from: ".github/workflows/ci.yml"
      to: "cargo build --release + cargo test"
      via: "GHA steps on windows-latest"
      pattern: "cargo (build|test)"
---

<objective>
Scaffold the Brevly Print Cargo project as a lib+bin crate (D-06), pin the full v1
dependency set at the RESEARCH.md-verified versions (D-19), stand up the `%APPDATA%\BrevlyPrint\`
directory initializer (D-17), create the empty module skeletons + Wave 0 test files that the
Wave 2 persistence plan fills in, and wire the Windows CI gate (D-03).

Purpose: Proves SC-4 (full dep set compiles on Windows) and SC-2's directory half, and lays
the lib+bin foundation + test scaffolds so the persistence and event-loop plans have compiling
contracts to implement against — no scavenger hunt.

Output: A compiling `cargo build --release` on the Windows target, a green `cargo test app_dir`,
the three `src/*_store.rs` / `app_dir.rs` module stubs, the two Wave 0 integration test files
(compiling, initially covering only what exists), and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-thread-model-spike/01-CONTEXT.md
@.planning/phases/01-foundation-thread-model-spike/01-RESEARCH.md
@.planning/phases/01-foundation-thread-model-spike/01-VALIDATION.md

<interfaces>
<!-- Contracts this plan establishes for Wave 2 + Wave 3 to implement against. -->
<!-- These are the lib crate's public surface. The Wave 2 plan implements the bodies. -->

Lib crate name: `brevly_print` (package name `brevly-print`; underscore in the crate path).

src/app_dir.rs  (fully implemented in THIS plan — RESEARCH.md Pattern 5):
  pub fn init_app_dir() -> Result<std::path::PathBuf, std::io::Error>
    // Resolves dirs::data_dir()/"BrevlyPrint", create_dir_all, returns the PathBuf.

src/config_store.rs  (skeleton in THIS plan; bodies filled by Wave 2 plan 02):
  pub struct ConfigStore { /* holds rusqlite::Connection */ }
  pub const MIGRATIONS: rusqlite_migration::Migrations<'static>  // v1 = 3 tables (Wave 2)
  pub fn open_and_migrate(path: &std::path::Path) -> rusqlite::Result<rusqlite::Connection>

src/credential_store.rs  (skeleton in THIS plan; bodies filled by Wave 2 plan 02):
  pub enum CredentialError { NotFound, Corrupt(anyhow::Error), Io(std::io::Error) }  // thiserror, D-18
  pub struct CredentialStore { /* holds PathBuf */ }
  impl CredentialStore { pub fn new(app_dir: &std::path::Path) -> Self; save(&str); load() -> Result<String, CredentialError> }

src/lib.rs re-exports: pub mod app_dir; pub mod config_store; pub mod credential_store;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo lib+bin scaffold with full v1 dependency set</name>
  <files>Cargo.toml, .gitignore, src/lib.rs, src/main.rs</files>
  <read_first>
    - CLAUDE.md (Technology Stack tables — the recommended crates)
    - .planning/phases/01-foundation-thread-model-spike/01-RESEARCH.md §Standard Stack (the AUTHORITATIVE verified version table) and §"Full v1 Dependency Set (D-19)"
    - 01-CONTEXT.md D-02 (target triple), D-06 (lib+bin split), D-19 (full dep set, verified versions)
  </read_first>
  <action>
    Run `cargo init --lib` semantics manually via the Write tool (do NOT scaffold with a bin-only
    template): create Cargo.toml declaring BOTH a `[lib]` (name `brevly_print`, path `src/lib.rs`)
    and a `[[bin]]` (name `brevly-print`, path `src/main.rs`), package name `brevly-print`,
    edition 2024, rust-version pinned per RESEARCH.md (Rust 1.97 stable). Populate `[dependencies]`
    with the COMPLETE v1 set from 01-RESEARCH.md §"Full v1 Dependency Set (D-19)" — ALL crates,
    at the versions in the §Standard Stack table, EXACTLY: winit 0.30.13, egui 0.35,
    egui-wgpu 0.35 (features ["winit"]), egui-winit 0.35, wgpu 29 (default-features=false,
    features ["wgsl","dx12"]), tray-icon 0.24, the windows crate 0.62 with features
    ["Win32_Graphics_Printing","Win32_Foundation"], printers 2, serialport 4.9, tokio 1
    (features ["full"]), reqwest 0.13 (default-features=false, features ["rustls-tls","json"]),
    tokio-tungstenite 0.30 (features ["rustls-tls-webpki-roots"]), hmac 0.13, sha2 0.11,
    rusqlite 0.40 (features ["bundled"]), rusqlite_migration 2.6, serde 1 (features ["derive"]),
    serde_json 1, dirs 6, windows-dpapi 0.2, auto-launch 0.6, velopack 1,
    tauri-winrt-notification 0.8, thiserror 2, anyhow 1. Do NOT add `tao` or `eframe`
    (prohibited per D-08 / Anti-Patterns). Make `src/main.rs` a minimal stub that calls
    `brevly_print::app_dir::init_app_dir()` and prints the path then exits 0 (real event loop
    arrives in plan 03). Make `src/lib.rs` declare `pub mod app_dir; pub mod config_store;
    pub mod credential_store;`. Write a Rust `.gitignore` (/target, Cargo.lock stays committed
    per RESEARCH.md project structure — do NOT ignore Cargo.lock).
  </action>
  <verify>
    <automated>cargo build --release --target x86_64-pc-windows-msvc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release --target x86_64-pc-windows-msvc` exits 0 on the Windows box / GHA runner.
    - `Cargo.toml` contains a `[lib]` with `name = "brevly_print"` and a `[[bin]]` with `name = "brevly-print"`.
    - `Cargo.toml` contains `winit`, `egui-wgpu`, `wgpu`, `rusqlite`, `rusqlite_migration`, `windows-dpapi`, `velopack`, `tauri-winrt-notification` (grep -v '^#' Cargo.toml | grep -c 'winit\|wgpu\|rusqlite\|windows-dpapi' returns >= 4).
    - `Cargo.toml` does NOT contain a dependency line for `tao` or `eframe` (grep -v '^#' Cargo.toml | grep -cE '^(tao|eframe)\s*=' returns 0).
    - `.gitignore` ignores `/target` and does NOT ignore `Cargo.lock`.
  </acceptance_criteria>
  <done>The full v1 dep set compiles as a lib+bin crate on the Windows target; no tao/eframe present.</done>
</task>

<task type="auto">
  <name>Task 2: init_app_dir() with idempotency test + module skeletons for Wave 2</name>
  <files>src/app_dir.rs, src/config_store.rs, src/credential_store.rs, src/lib.rs</files>
  <read_first>
    - src/lib.rs (from Task 1)
    - 01-RESEARCH.md §Pattern 5 (init_app_dir via dirs::data_dir + create_dir_all) and §Pattern 3 (CredentialError shape) and §Pattern 4 (open_and_migrate signature)
    - 01-CONTEXT.md D-17 (create_dir_all before any file op), D-18 (thiserror typed errors), D-14 (schema shapes — not implemented here, just the ConfigStore struct)
    - 01-VALIDATION.md §Per-Task Verification Map row "%APPDATA%\BrevlyPrint\ created idempotently"
  </read_first>
  <action>
    Implement `src/app_dir.rs` fully per RESEARCH.md Pattern 5: `pub fn init_app_dir() ->
    Result<std::path::PathBuf, std::io::Error>` resolving `dirs::data_dir()` joined with
    "BrevlyPrint", mapping a None base to `std::io::ErrorKind::NotFound`, then `create_dir_all`
    (idempotent). Add an inline `#[cfg(test)] mod tests` that: overrides the base to a
    `std::env::temp_dir()` subpath (extract the join+create logic into a private
    `create_in(base: &Path) -> io::Result<PathBuf>` helper so the test does not depend on the
    real %APPDATA%), asserts the dir exists after one call, and asserts a SECOND call returns Ok
    (idempotency, no error). Create `src/credential_store.rs` with the typed `CredentialError`
    enum (thiserror, variants NotFound / Corrupt(#[source] anyhow::Error) / Io(#[from]
    std::io::Error) per Pattern 3) and a `CredentialStore { path: PathBuf }` struct with a
    `new(app_dir: &Path)` constructor plus `save`/`load` method signatures that currently
    `todo!()` (Wave 2 plan 02 implements the bodies). Create `src/config_store.rs` with a
    `pub struct ConfigStore` wrapping an `rusqlite::Connection`, a `pub const MIGRATIONS:
    rusqlite_migration::Migrations<'static>` placeholder (empty slice for now — Wave 2 adds the
    v1 tables), and a `pub fn open_and_migrate(path: &Path) -> rusqlite::Result<Connection>`
    signature returning a bare opened connection for now. Keep every stub compiling; use `todo!()`
    only inside method bodies that Wave 2 fills, never in signatures/const definitions that must
    compile. Ensure `src/lib.rs` re-exports the modules.
  </action>
  <verify>
    <automated>cargo test --target x86_64-pc-windows-msvc app_dir::tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --target x86_64-pc-windows-msvc app_dir::tests` passes (idempotency test green).
    - `src/app_dir.rs` contains `pub fn init_app_dir`.
    - `src/credential_store.rs` contains `pub enum CredentialError` with `NotFound` and `Corrupt` variants (grep -c 'NotFound\|Corrupt' >= 2) and derives `thiserror::Error`.
    - `src/config_store.rs` contains `pub struct ConfigStore` and `pub const MIGRATIONS`.
    - `cargo build --target x86_64-pc-windows-msvc` still exits 0 (all skeletons compile).
  </acceptance_criteria>
  <done>init_app_dir() creates the dir idempotently with a passing test; store skeletons compile as the Wave 2 contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 integration test scaffolds + Windows CI gate</name>
  <files>tests/config_store_test.rs, tests/credential_store_test.rs, .github/workflows/ci.yml</files>
  <read_first>
    - src/config_store.rs, src/credential_store.rs, src/app_dir.rs (contracts from Task 2)
    - 01-VALIDATION.md §"Wave 0 Requirements" and §"Per-Task Verification Map" (the exact test names: test_write_read, test_round_trip, test_missing_file, test_corrupt_blob)
    - 01-RESEARCH.md §Pattern 6 (the ci.yml content, WGPU_BACKEND=dx12, RUST_TEST_THREADS=1)
    - 01-CONTEXT.md D-03 (build-only CI, no signing)
  </read_first>
  <action>
    Create `tests/config_store_test.rs` as the Wave 0 SQLite integration scaffold: a `test_write_read`
    test (currently marked `#[ignore = "impl in wave 2"]` since the schema is a stub) plus a
    `test_schema_v1_user_version` test also `#[ignore]`-gated — both compile and reference
    `brevly_print::config_store::open_and_migrate` / `ConfigStore` so the contract is exercised at
    compile time. Create `tests/credential_store_test.rs` gated with `#![cfg(target_os = "windows")]`
    at file top (per VALIDATION.md — DPAPI is Windows-only) containing `test_round_trip`,
    `test_missing_file`, `test_corrupt_blob`, each `#[ignore = "impl in wave 2"]`, referencing
    `brevly_print::credential_store::{CredentialStore, CredentialError}` so the types are checked
    now. Wave 2 plan 02 removes the `#[ignore]` markers and fills the assertions. Create
    `.github/workflows/ci.yml` EXACTLY per RESEARCH.md Pattern 6: name CI, triggers push(main) +
    pull_request, `env: WGPU_BACKEND: dx12` and `CARGO_TERM_COLOR: always`, one job
    `build-windows` on `windows-latest` with steps: checkout@v4, dtolnay/rust-toolchain@stable
    (targets x86_64-pc-windows-msvc), Swatinem/rust-cache@v2, `cargo build --release --target
    x86_64-pc-windows-msvc`, then `cargo test --target x86_64-pc-windows-msvc` with
    `RUST_TEST_THREADS: 1` in the test step env. Do NOT add any signing, packaging, or vpk steps
    (D-03 — build-only; signing is Phase 3).
  </action>
  <verify>
    <automated>cargo test --target x86_64-pc-windows-msvc --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --target x86_64-pc-windows-msvc --no-run` compiles all integration tests (exit 0).
    - `tests/credential_store_test.rs` first line is `#![cfg(target_os = "windows")]`.
    - `tests/credential_store_test.rs` references `test_round_trip`, `test_missing_file`, `test_corrupt_blob` (grep -c returns 3).
    - `.github/workflows/ci.yml` contains `runs-on: windows-latest`, `WGPU_BACKEND: dx12`, `cargo build --release`, and `cargo test`.
    - `.github/workflows/ci.yml` contains NO `signtool`, `vpk`, or `sign` steps (grep -ci 'signtool\|vpk\| sign ' returns 0).
  </acceptance_criteria>
  <done>Both Wave 0 test files compile against the store contracts; the build-only Windows CI gate is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → agent | Reading/creating `%APPDATA%\BrevlyPrint\`; only the current Windows user's profile is touched. No network, no external input in this plan. |
| CI runner → repo | GHA Windows runner executes `cargo build`/`cargo test`; supply-chain surface is the crate dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01a | Denial of Service | `init_app_dir()` cannot resolve `%APPDATA%` | mitigate | `dirs::data_dir()` None → typed `io::Error` (NotFound), never panic (Pattern 5). Corrupt-credential path (T-1-01) lands in plan 02. |
| T-1-SC | Tampering | cargo dependency installs (crates.io) | mitigate | RESEARCH.md §Package Legitimacy Audit verified all 24 crates on crates.io (VERIFIED). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking human checkpoint required. `Cargo.lock` committed to pin the resolved graph. |

Most of this plan (Cargo scaffold, module stubs, CI YAML) has no external attack surface — stated plainly rather than inventing threats.
</threat_model>

<verification>
- `cargo build --release --target x86_64-pc-windows-msvc` exits 0 (SC-4 proven).
- `cargo test --target x86_64-pc-windows-msvc app_dir::tests` green (SC-2 dir half).
- `cargo test --target x86_64-pc-windows-msvc --no-run` compiles all Wave 0 test files.
- GHA `build-windows` job runs on push (CI gate live).
</verification>

<success_criteria>
- SC-4: full v1 dependency set compiles for `x86_64-pc-windows-msvc`.
- SC-2 (partial): `%APPDATA%\BrevlyPrint\` created idempotently; test green.
- lib+bin split established (`brevly_print` lib + `brevly-print` bin).
- Wave 0 test files compile against the store contracts (unblocks plan 02).
- Windows build-only CI committed (unblocks the phase gate).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-thread-model-spike/01-01-SUMMARY.md` when done.
</output>
