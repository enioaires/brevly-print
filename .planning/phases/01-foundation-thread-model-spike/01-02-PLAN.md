---
phase: 01-foundation-thread-model-spike
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/config_store.rs
  - src/credential_store/dpapi.rs
  - src/credential_store/devfile.rs
  - tests/config_store_test.rs
  - tests/credential_store_test.rs
  - tests/credential_contract_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "SQLite state.db initializes with tables config, printed_jobs, retry_queue (migration v1, user_version=1)"
    - "A config key/value row can be written and read back"
    - "Credentials round-trip through the CredentialStore trait; missing → NotFound, corrupt → Corrupt, never panics"
    - "The trait + error contract is proven on Linux (DevFileCredentialStore); the real DPAPI round-trip is proven on Windows"
  artifacts:
    - path: "src/config_store.rs"
      provides: "rusqlite_migration v1 (3 tables) + get/set on config"
      contains: "Migrations"
    - path: "tests/config_store_test.rs"
      provides: "Linux-runnable SQLite schema + write/read integration tests"
      contains: "user_version"
    - path: "tests/credential_contract_test.rs"
      provides: "Linux-runnable CredentialStore trait + CredentialError contract test"
      contains: "CredentialError"
    - path: "tests/credential_store_test.rs"
      provides: "Windows-only real DPAPI round-trip / missing / corrupt tests"
      contains: "cfg(target_os = \"windows\")"
  key_links:
    - from: "src/config_store.rs"
      to: "rusqlite_migration"
      via: "MIGRATIONS.to_latest(&mut conn)"
      pattern: "to_latest"
    - from: "tests/config_store_test.rs"
      to: "brevly_print::config_store"
      via: "open_and_migrate on a temp db path"
      pattern: "open_and_migrate"
---

<objective>
Fill the two persistence contracts scaffolded in 01-01 and make their Wave-0 tests green:
the SQLite `ConfigStore` (versioned migration v1 = the three D-14 tables + key/value get/set)
and the two `CredentialStore` impls (real DPAPI on Windows, dev-file on Linux). This closes
SC-2 (SQLite schema on the per-platform app dir) and SC-3 (typed credential round-trip, never
panics) — provable on Linux for everything portable, with the real DPAPI path proven on
Windows CI.

Purpose: These are the persistence primitives every later phase depends on (dedup fence,
retry queue, agentToken storage). Getting the migration + typed-error contract right now,
tested on both platforms, is the whole point of the spike's infra half.

Output: A migrating `state.db`, get/set on `config`, both credential impls satisfying the
trait with typed errors, and green integration tests (Linux subset + Windows subset).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-thread-model-spike/01-CONTEXT.md
@.planning/phases/01-foundation-thread-model-spike/01-RESEARCH.md
@.planning/phases/01-foundation-thread-model-spike/01-VALIDATION.md
@.planning/phases/01-foundation-thread-model-spike/01-01-SUMMARY.md

<decisions_implemented>
D-12 (rusqlite_migration versioned, user_version), D-13 (state.db in app dir, bundled),
D-14 (schema v1: config / printed_jobs / retry_queue), D-15 (2 credential impls),
D-16 (CredentialError never panics), D-22 (Linux tests everything portable; DPAPI Windows-gated).
</decisions_implemented>

<interfaces>
<!-- Contracts DEFINED in 01-01 that this plan IMPLEMENTS. Do not redesign signatures. -->
config_store::open_and_migrate(path: &Path) -> rusqlite::Result<Connection>
config_store::set(conn: &Connection, key: &str, value: &str) -> rusqlite::Result<()>
config_store::get(conn: &Connection, key: &str) -> rusqlite::Result<Option<String>>
credential_store::CredentialStore { save(&[u8]) -> Result<(),CredentialError>; load() -> Result<Vec<u8>,CredentialError> }
credential_store::credential_store(app_dir: &Path) -> impl CredentialStore   // cfg-selected
CredentialError { NotFound, Corrupt(anyhow::Error), Io(std::io::Error) }
</interfaces>

Reference patterns: RESEARCH Pattern 4 (rusqlite_migration v1 schema — the exact 3-table
`M::up`), Pattern 3 (DPAPI save/load + error mapping), Pitfall 5 (map windows-dpapi anyhow
error to typed CredentialError; existence-check before decrypt), Pitfall 7 (multi-statement
`M::up` fallback → split into separate `M::up` calls if execute_batch chokes), Open Question 1
(RESOLVED — runtime split fallback).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ConfigStore (migration v1, get/set) + fill config_store_test on Linux</name>
  <read_first>
    - 01-RESEARCH.md Pattern 4 (rusqlite_migration v1 — the exact 3-table M::up + open_and_migrate),
      Pitfall 4 (create_dir_all before open), Pitfall 7 (multi-statement M::up split fallback),
      Open Question 1 (RESOLVED)
    - 01-CONTEXT.md D-12, D-13, D-14 (exact column shapes for config / printed_jobs / retry_queue)
    - src/config_store.rs (the stub from 01-01/T3 — replace todo!() bodies)
    - tests/config_store_test.rs (the #[ignore] scaffold from 01-01/T3 — fill assertions, un-ignore)
  </read_first>
  <behavior>
    - open_and_migrate on a fresh db creates config, printed_jobs, retry_queue and sets user_version = 1
    - set('printer_name','X') then get('printer_name') returns Some("X")
    - get on an absent key returns Ok(None), not an error
    - Re-opening an already-migrated db is a no-op (user_version stays 1, no error)
  </behavior>
  <action>
    Replace the `src/config_store.rs` stub bodies with the real implementation per RESEARCH
    Pattern 4: a module-level `const MIGRATIONS: Migrations` (or `LazyLock`) holding a single
    `M::up` for v1 with the three D-14 tables — `config(key TEXT PRIMARY KEY NOT NULL, value
    TEXT NOT NULL)`, `printed_jobs(job_id TEXT PK, job_type TEXT, status TEXT DEFAULT 'pending'
    CHECK(...), attempt INTEGER DEFAULT 0, received_at TEXT, printed_at TEXT, failed_at TEXT)`
    + `CREATE INDEX idx_printed_jobs_status`, `retry_queue(job_id TEXT PK REFERENCES
    printed_jobs, job_type TEXT, escpos_bytes BLOB, attempt_count INTEGER DEFAULT 0,
    next_retry_at TEXT, last_error TEXT, created_at TEXT)`. `open_and_migrate` opens the
    connection and calls `MIGRATIONS.to_latest(&mut conn)`. If the single multi-statement
    `M::up` fails at runtime (Pitfall 7 / OQ1), split into separate `M::up` calls within the
    SAME v1 migration — document which path was taken in the SUMMARY. Implement `set` via
    `INSERT ... ON CONFLICT(key) DO UPDATE`, `get` via `query_row` returning
    `Option<String>` (map `QueryReturnedNoRows` → `Ok(None)`). Fully portable — no cfg gates.

    Fill `tests/config_store_test.rs` (remove `#[ignore]`): open a db in a `tempfile::tempdir()`,
    assert the 3 tables exist (query `sqlite_master`), assert `PRAGMA user_version` == 1,
    write a `config` row and read it back, assert absent key → None, assert a second
    `open_and_migrate` on the same file still yields user_version 1. Runs on Linux.
  </action>
  <verify>
    <automated>cargo test --test config_store_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test config_store_test` passes on Linux (SC-2)
    - `src/config_store.rs` contains a `Migrations` with the 3 tables and `to_latest`
    - Test asserts `PRAGMA user_version` returns 1 after migration
    - Test asserts config write→read round-trip and absent-key → Ok(None)
    - No `#[ignore]` remains in `tests/config_store_test.rs`
  </acceptance_criteria>
  <done>SQLite schema v1 initializes and a config row round-trips — proven on Linux.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement both CredentialStore impls + fill Linux contract test + Windows DPAPI tests</name>
  <read_first>
    - 01-RESEARCH.md Pattern 3 (DPAPI save/load + CredentialError mapping), Pitfall 5
      (windows-dpapi returns anyhow; map to typed error; existence-check before decrypt),
      §"Cross-Platform Structure (Addendum)" (DevFile impl is dev-only, contract tested on both)
    - 01-CONTEXT.md D-15, D-16 (NotFound vs Corrupt, never panic), D-22 (Linux vs Windows test split), D-24
    - src/credential_store/dpapi.rs + devfile.rs (the impls from 01-01/T3 — complete the bodies)
    - tests/credential_store_test.rs (#![cfg(target_os="windows")] scaffold), tests/credential_contract_test.rs (Linux scaffold)
  </read_first>
  <behavior>
    - Windows DpapiCredentialStore: save(b"tok") then load() returns b"tok" (Scope::User round-trip)
    - Missing credential.bin → CredentialError::NotFound (both impls), never a panic
    - Corrupt/undecryptable blob → CredentialError::Corrupt (Windows real DPAPI), never a panic
    - Linux DevFileCredentialStore: save→load round-trips; missing → NotFound — proves the trait+error contract on Linux
  </behavior>
  <action>
    Complete `src/credential_store/dpapi.rs` (`#![cfg(windows)]`) per RESEARCH Pattern 3:
    `save` = `windows_dpapi::encrypt_data(secret, Scope::User, None)` → write to `credential.bin`;
    `load` = existence-check (`path.exists()` false → `CredentialError::NotFound`) → read →
    `decrypt_data(&bytes, Scope::User, None)` mapping the `anyhow` error to
    `CredentialError::Corrupt` (Pitfall 5). Complete `src/credential_store/devfile.rs`
    (`#![cfg(not(windows))]`): `save` = write bytes to `credential.bin`; `load` =
    existence-check → NotFound, else read → Ok(bytes); keep the DEV/TEST-ONLY doc-comment.
    Neither impl panics on any input (D-16).

    Fill `tests/credential_contract_test.rs` (Linux-runnable, NOT windows-gated) — remove
    `#[ignore]`: construct a `DevFileCredentialStore` in a `tempfile::tempdir()`, assert
    `load()` on empty dir → `Err(CredentialError::NotFound)` (matches!), then `save(b"dummy")`
    → `load()` returns `b"dummy"`. This proves the trait + error contract on Linux (SC-3
    Linux half).

    Fill `tests/credential_store_test.rs` (`#![cfg(target_os = "windows")]`) — remove
    `#[ignore]`: `test_round_trip` (save→load equal, real DPAPI), `test_missing_file`
    (fresh temp dir → NotFound), `test_corrupt_blob` (write random bytes to credential.bin →
    load → matches `CredentialError::Corrupt`). These run only on the Windows box / Windows CI
    (D-22) and are the real DPAPI proof (SC-3 Windows half, T-1-01).
  </action>
  <verify>
    <automated>cargo test --test credential_contract_test 2>&1 | tail -8 && cargo test --test credential_store_test --no-run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test credential_contract_test` passes on Linux (trait + CredentialError contract, SC-3 Linux half)
    - `cargo test --test credential_store_test --no-run` compiles on Linux (Windows-gated body is skipped, not broken)
    - `src/credential_store/dpapi.rs` maps decrypt failure to `CredentialError::Corrupt` and missing file to `NotFound`
    - No impl panics on missing/corrupt input (matches! on the typed variants)
    - `tests/credential_store_test.rs` real DPAPI tests are `#[cfg(target_os = "windows")]` (Windows CI proves round-trip/corrupt)
  </acceptance_criteria>
  <done>Both credential impls satisfy the trait with typed errors; contract green on Linux, real DPAPI green on Windows CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk ↔ app | `state.db` + `credential.bin` in the per-user app dir; no network in Phase 1 |
| Windows user session ↔ DPAPI | `Scope::User` binds the credential blob to the Windows account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure / DoS | `DpapiCredentialStore` | mitigate | `Scope::User` DPAPI (real round-trip test on Windows); undecryptable blob (DPAPI key loss M7) → `CredentialError::Corrupt`, never panic — the exact hook Phase 2 uses to re-enter activation. Verified by `test_corrupt_blob` (Windows). |
| T-1-02 | Information Disclosure | `DevFileCredentialStore` (Linux) | accept (non-shipping) | Plaintext file, DEV/TEST ONLY, `#[cfg(not(windows))]`, never ships (D-24). Exists solely to test the trait/error contract on Linux. Documented non-shipping constraint. |
| T-1-03 | Tampering | `state.db` schema evolution | mitigate | `rusqlite_migration` ordered `user_version` migrations (D-12) — the only safe upgrade path for already-deployed field agents; `CREATE TABLE IF NOT EXISTS` rejected. |
</threat_model>

<verification>
- Linux: `cargo test --test config_store_test` and `cargo test --test credential_contract_test` pass; `cargo test --test credential_store_test --no-run` compiles.
- Windows (CI): `cargo test` runs the real DPAPI round-trip/missing/corrupt tests green.
</verification>

<success_criteria>
- SC-2 proven on Linux: SQLite schema v1 (3 tables, user_version=1) + config write/read.
- SC-3 (Linux half) proven: CredentialStore trait + CredentialError contract via DevFile.
- SC-3 (Windows half) wired: real DPAPI round-trip/corrupt tests run on Windows CI (T-1-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-thread-model-spike/01-02-SUMMARY.md` when done.
Record: whether the single multi-statement `M::up` worked or was split (Pitfall 7 / OQ1), and
confirm which credential tests ran on Linux vs are Windows-CI-only.
</output>
