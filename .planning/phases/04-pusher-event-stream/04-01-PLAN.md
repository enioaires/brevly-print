---
phase: 04-pusher-event-stream
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/pusher/mod.rs
  - src/pusher/protocol.rs
  - src/pusher/backoff.rs
  - src/noren_client.rs
  - src/lib.rs
  - src/activation_window.rs
  - tests/pusher_auth_test.rs
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03]
must_haves:
  truths:
    - "pusher_auth() returns Ok(auth_string) on 200 and Err on 403/transport"
    - "parse_print_job() double-decodes the JSON-in-JSON data field into {jobId, type}"
    - "extract_socket_id() reads socket_id from the connection_established envelope"
    - "backoff_delay(attempt) never exceeds the 60s cap for any attempt"
    - "Activation save persists pusher_key and pusher_cluster to ConfigStore (D-01)"
  artifacts:
    - path: "src/pusher/protocol.rs"
      provides: "PusherEnvelope, PrintEvent, PusherConfig, parse_print_job, extract_socket_id"
      contains: "pub struct PrintEvent"
    - path: "src/pusher/backoff.rs"
      provides: "backoff_delay with jitter, 60s cap"
      contains: "pub fn backoff_delay"
    - path: "src/pusher/mod.rs"
      provides: "pusher module index"
      contains: "pub mod protocol"
    - path: "src/noren_client.rs"
      provides: "pusher_auth() channel-auth POST"
      contains: "pub async fn pusher_auth"
    - path: "tests/pusher_auth_test.rs"
      provides: "HTTP contract tests for pusher_auth (EVT-01, EVT-02)"
      contains: "channel_name"
  key_links:
    - from: "src/lib.rs"
      to: "src/pusher/mod.rs"
      via: "pub mod pusher"
      pattern: "pub mod pusher"
    - from: "src/activation_window.rs"
      to: "config_store::set"
      via: "pusher_key / pusher_cluster config entries"
      pattern: "pusher_key"
---

<objective>
Build the tested primitives the Pusher reconnect loop consumes: the Pusher protocol
types + parsers (`protocol.rs`), the exponential-backoff function (`backoff.rs`), the
`pusher_auth()` channel-auth POST (added to `noren_client.rs`), the `pusher` module index,
and its `lib.rs` registration. Also close the D-01 dependency by persisting `pusher_key`
and `pusher_cluster` to `ConfigStore` at activation time (Phase 2's save loop currently
omits them). Add the explicit `futures-util` and `rand` dependencies.

Purpose: These are the contracts Plan 02's reconnect loop is built against. Delivering
them first (with unit + integration tests) means Plan 02 receives real signatures, not a
scavenger hunt. Covers the tested seams of EVT-01 (double-decode + auth), EVT-02 (fresh
auth POST contract), EVT-03 (backoff cap).

Output: `src/pusher/{mod,protocol,backoff}.rs`, `pusher_auth()` in `noren_client.rs`,
`pub mod pusher` in `lib.rs`, two new config entries in `activation_window.rs`,
`tests/pusher_auth_test.rs`, and two new `Cargo.toml` dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pusher-event-stream/04-CONTEXT.md
@.planning/phases/04-pusher-event-stream/04-RESEARCH.md
@.planning/phases/04-pusher-event-stream/04-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/noren_client.rs (existing patterns to replicate for pusher_auth):
- `pub fn noren_base_url() -> String` — resolves base URL via option_env!("NOREN_BASE_URL")
- `pub async fn activate(client: &reqwest::Client, base_url: &str, serial: &str, machine_id: Option<&str>, force_rebind: bool) -> Result<ActivateResponse, ActivateError>`
- `ActivateResponse { agent_token, tenant_id, pusher_key, pusher_cluster, enabled_types }` (camelCase-renamed)
- Module uses `use serde::{Deserialize, Serialize}` and `thiserror`. It does NOT currently import `anyhow`.

From src/config_store.rs:
- `pub fn set(conn: &Connection, key: &str, value: &str) -> rusqlite::Result<()>`
- `printed_jobs` table schema: `job_id TEXT PRIMARY KEY, job_type TEXT, status TEXT DEFAULT 'pending' CHECK(status IN ('pending','printed','failed')), attempt INTEGER, received_at TEXT, printed_at TEXT, failed_at TEXT`

From src/activation_window.rs (the save loop that needs the D-01 fix, ~line 977):
- `config_entries: Vec<(&str, String)>` currently holds: printer_name, printer_type, tenant_id, enabled_types, noren_base_url.
- `state.pusher_key: Option<String>` and `state.pusher_cluster: Option<String>` are already populated from ActivateResponse (~line 421-422).

From src/lib.rs — module registration list (add `pub mod pusher;` alongside these):
- pub mod activation_state; ... pub mod noren_client; pub mod printer; pub mod tray_runtime;

From tests/noren_client_test.rs — the mock TCP stub pattern:
- `async fn spawn_stub(status: u16, body: &'static str) -> String` returns `http://127.0.0.1:{port}`; test uses `#[tokio::test]` and `reqwest::Client::new()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add futures-util + rand deps and persist pusher_key/pusher_cluster at activation (D-01)</name>
  <files>Cargo.toml, src/activation_window.rs</files>
  <read_first>
    - Cargo.toml (see the `[dependencies]` block — futures-util and rand are NOT present; futures-util is only transitive via tokio-tungstenite)
    - src/activation_window.rs (read the save loop around lines 960-995 where `config_entries` is built and passed to `config_store::set`, and lines 415-425 where `state.pusher_key`/`state.pusher_cluster` are set from the ActivateResponse)
    - .planning/phases/04-pusher-event-stream/04-CONTEXT.md (D-01 — Phase 4 reads pusher_key/pusher_cluster from ConfigStore at startup; Phase 2's save must persist them)
  </read_first>
  <action>
    In Cargo.toml `[dependencies]`, add `futures-util = "0.3"` (Pitfall 6 — must be an explicit
    dep so StreamExt/SinkExt are referenceable in Plan 02; the resolved lockfile version is 0.3.32)
    and `rand = "0.8"` (used by backoff jitter in Task 2). Place both in the portable
    `[dependencies]` section (NOT the Windows-only target block) since the pusher module is
    cross-platform.

    In src/activation_window.rs, extend the `config_entries` vec (currently printer_name,
    printer_type, tenant_id, enabled_types, noren_base_url) to also include two new entries:
    `("pusher_key", <value from state.pusher_key>)` and `("pusher_cluster", <value from
    state.pusher_cluster>)`. Read both from the already-populated `state.pusher_key` /
    `state.pusher_cluster` Option<String> fields using the same `.clone().unwrap_or_default()`
    approach used for `tenant_id` on the line above. This is the D-01 fence: without it,
    Plan 02's Runtime-mode startup cannot read `pusher_key`/`pusher_cluster` from ConfigStore
    and the Pusher connection has no app key.
  </action>
  <verify>
    <automated>grep -q 'futures-util = "0.3"' Cargo.toml && grep -q 'rand = "0.8"' Cargo.toml && grep -q '"pusher_key"' src/activation_window.rs && grep -q '"pusher_cluster"' src/activation_window.rs && cargo build --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `futures-util = "0.3"` in the `[dependencies]` section (not under `[target.'cfg(windows)'.dependencies]`)
    - `Cargo.toml` contains `rand = "0.8"` in `[dependencies]`
    - `src/activation_window.rs` `config_entries` vec contains a `("pusher_key", ...)` tuple and a `("pusher_cluster", ...)` tuple sourced from `state.pusher_key` / `state.pusher_cluster`
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>futures-util and rand are explicit deps; activation save persists pusher_key + pusher_cluster to ConfigStore (D-01 satisfied); lib builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pusher protocol types + parsers and backoff, with inline tests</name>
  <files>src/pusher/mod.rs, src/pusher/protocol.rs, src/pusher/backoff.rs, src/lib.rs</files>
  <behavior>
    - parse_print_job: given a PusherEnvelope whose `data` is the JSON STRING `"{\"jobId\":\"abc\",\"type\":\"order\"}"`, returns PrintEvent { job_id: "abc", job_type: "order" }
    - parse_print_job: given an envelope whose `data` is a JSON object (not a string), returns Err (defensive double-decode contract)
    - extract_socket_id: given data string `"{\"socket_id\":\"123.456\",\"activity_timeout\":120}"`, returns "123.456"
    - backoff_delay: for every attempt in 0..=20, the returned Duration is <= 60_000 ms (cap holds even with +25% jitter)
    - backoff_delay(0) is within the jittered range of 1s base (>= 750ms, <= 1250ms)
  </behavior>
  <read_first>
    - src/health_state.rs (the analog: portable module + `#[cfg(test)] mod tests` with pure-function unit tests, no async/no I/O)
    - src/printer/mod.rs (analog for the module index pub-use pattern)
    - src/lib.rs (the module registration list — add `pub mod pusher;`)
    - .planning/phases/04-pusher-event-stream/04-PATTERNS.md (protocol.rs section: PusherEnvelope/PrintEvent/PusherConfig struct definitions, double-decode pattern, extract_socket_id; backoff.rs section)
    - .planning/phases/04-pusher-event-stream/04-RESEARCH.md (Pattern 3 double-decode, Pattern 4 backoff with jitter, Code Examples section for envelope + connection_established types)
  </read_first>
  <action>
    Create `src/pusher/mod.rs` as a flat pub-use index (NO `#[cfg(windows)]` gate — the module is
    cross-platform): declare `pub mod client;` (Plan 02 fills it), `pub mod protocol;`,
    `pub mod backoff;`, and re-export `pub use protocol::{PrintEvent, PusherConfig};`. Since
    `client.rs` does not exist yet, either create an empty `src/pusher/client.rs` placeholder now
    with a module doc comment so `pub mod client;` compiles, OR omit the `pub mod client;` line and
    the `run_pusher_loop` re-export until Plan 02 (choose the placeholder approach so lib.rs
    registration is stable across waves).

    Create `src/pusher/protocol.rs` (portable, no cfg gate). Define: `PusherEnvelope { event:
    String, data: serde_json::Value, #[serde(default)] channel: Option<String> }` (derive
    Deserialize, Debug); `PrintEvent { #[serde(rename="jobId")] job_id: String,
    #[serde(rename="type")] job_type: String }` (derive Deserialize, Debug, Clone); `PusherConfig
    { key: String, cluster: String, tenant_id: String, auth_url: String }` (derive Clone). Add
    `pub fn parse_print_job(env: &PusherEnvelope) -> anyhow::Result<PrintEvent>` implementing the
    double-decode: match `env.data` — if `serde_json::Value::String(s)`, run
    `serde_json::from_str::<PrintEvent>(s)`; any non-string variant returns Err (this is the
    Pitfall 3 defense). Add `pub fn extract_socket_id(env: &PusherEnvelope) -> anyhow::Result<String>`
    that reads `env.data.as_str()`, parses an inner `ConnectionEstablishedData { socket_id: String }`
    struct, and returns `socket_id`. Use `anyhow::Context` for error messages. Add
    `#[cfg(test)] mod tests` covering the five behaviors above.

    Create `src/pusher/backoff.rs` (portable). Define `pub fn backoff_delay(attempt: u32) ->
    std::time::Duration` per D-06: base = 1000ms * 2^attempt.min(6) (so base caps at 64s pre-cap),
    cap the base at 60_000ms, apply ±25% jitter via `rand::random::<f64>()` mapped to [0.75, 1.25],
    then cap the jittered result at 60_000ms so jitter never exceeds the ceiling. Add
    `#[cfg(test)] mod tests` asserting the cap (attempt 0..=20 all <= 60_000ms) and the attempt-0
    jitter range.

    In `src/lib.rs`, add `pub mod pusher;` alongside the existing module declarations.
  </action>
  <verify>
    <automated>cargo test --lib -- pusher::protocol::tests pusher::backoff::tests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/pusher/protocol.rs` contains `pub struct PrintEvent`, `pub struct PusherConfig`, `pub fn parse_print_job(`, `pub fn extract_socket_id(`
    - `src/pusher/backoff.rs` contains `pub fn backoff_delay`
    - `src/pusher/mod.rs` contains `pub mod protocol` and `pub mod backoff`
    - `src/lib.rs` contains `pub mod pusher;`
    - `cargo test --lib -- pusher::protocol::tests` exits 0 with the double-decode and socket_id tests passing
    - `cargo test --lib -- pusher::backoff::tests` exits 0 with `backoff_delay(attempt).as_millis() <= 60_000` for all attempts 0..=20
  </acceptance_criteria>
  <done>protocol.rs + backoff.rs exist with passing inline tests; pusher module registered in lib.rs; PrintEvent/PusherConfig re-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add pusher_auth() to noren_client with HTTP-contract tests (EVT-01, EVT-02)</name>
  <files>src/noren_client.rs, tests/pusher_auth_test.rs</files>
  <behavior>
    - pusher_auth on HTTP 200 with body `{"auth":"key123:hmac-abc"}` returns Ok("key123:hmac-abc")
    - pusher_auth on HTTP 403 returns Err (invalid token / channel mismatch)
    - pusher_auth on connection-refused (no listener) returns Err (transport)
    - The POST body uses the form field name `channel_name` (NOT `channel`) — verified by the fact that the function compiles the `.form(&[("channel_name", channel), ("socket_id", socket_id)])` call and the test asserts a 200 path succeeds against the stub
  </behavior>
  <read_first>
    - src/noren_client.rs (read the whole file — replicate the `activate()` structure: `format!("{base_url}/api/agent/...")`, shared `&reqwest::Client`, status-code match. NOTE: the module currently imports serde + thiserror but NOT anyhow — you must add `use anyhow::Context as _;` or use fully-qualified `anyhow::` calls)
    - tests/noren_client_test.rs (copy the `spawn_stub(status, body) -> String` mock TCP listener helper and the `#[tokio::test]` structure verbatim)
    - .planning/phases/04-pusher-event-stream/04-RESEARCH.md (Pattern 6 + Code Examples `pusher_auth` — the verified signature and status mapping; Pitfall 2 — channel_name not channel)
    - .planning/phases/04-pusher-event-stream/04-PATTERNS.md (noren_client.rs pusher_auth section)
  </read_first>
  <action>
    Add `pub async fn pusher_auth(client: &reqwest::Client, base_url: &str, agent_token: &str,
    channel: &str, socket_id: &str) -> anyhow::Result<String>` to `src/noren_client.rs`. It POSTs
    to `{base_url}/api/agent/pusher/auth` with `.bearer_auth(agent_token)` and a form body
    `.form(&[("channel_name", channel), ("socket_id", socket_id)])` — the field name MUST be
    `channel_name`, not `channel` (Pitfall 2, verified from Noren source). Define a function-local
    `#[derive(serde::Deserialize)] struct PusherAuthResponse { auth: String }`. Status mapping:
    200 → parse JSON, return `Ok(body.auth)`; 403 → `anyhow::bail!("pusher_auth: 403 — invalid
    token or channel mismatch")`; any other status → `anyhow::bail!("pusher_auth: unexpected status
    {status}")`. Use `.context(...)` on the `.send().await` and on `.json().await`. Do NOT log the
    Bearer token anywhere (T-02-02 / security V2). Add `use anyhow::Context as _;` at the top of
    the module if not present.

    Create `tests/pusher_auth_test.rs`. Copy the `spawn_stub` helper from
    tests/noren_client_test.rs. Import via `use brevly_print::noren_client::pusher_auth;`. Add
    three `#[tokio::test]` cases: (1) 200 body `{"auth":"key123:hmac-abc"}` → assert result unwraps
    to `"key123:hmac-abc"`; (2) 403 → assert result `.is_err()`; (3) connection refused (call
    pusher_auth against `http://127.0.0.1:1` or a port with no listener) → assert `.is_err()`. Pass
    a channel like `"private-tenant-t1-print"` and socket_id `"123.456"`.
  </action>
  <verify>
    <automated>cargo test --test pusher_auth_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/noren_client.rs` contains `pub async fn pusher_auth(` returning `anyhow::Result<String>`
    - `src/noren_client.rs` contains the literal `"channel_name"` in the `.form(...)` call (NOT a bare `"channel"` form field)
    - `tests/pusher_auth_test.rs` exists and contains three `#[tokio::test]` functions
    - `cargo test --test pusher_auth_test` exits 0 with all three cases passing
    - `grep -n bearer_auth src/noren_client.rs` shows the token is passed via `.bearer_auth()` (never string-formatted into a log line)
  </acceptance_criteria>
  <done>pusher_auth() maps 200/403/transport to typed results using channel_name; three integration tests green (EVT-01 auth contract, EVT-02 fresh-POST contract).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → Noren `/api/agent/pusher/auth` | agentToken (Bearer) crosses to the server; auth string returned |
| Noren → agent (WS envelope, next plan) | untrusted JSON envelope parsed by protocol.rs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | pusher_auth() Bearer token | mitigate | Token passed via `.bearer_auth()`; never string-formatted into any log/eprintln (V2, T-02-02) |
| T-04-02 | Elevation of Privilege | channel isolation | mitigate | Auth POST sends the tenant-scoped channel name; Noren hard-403s wrong channel (T-38-05). Agent surfaces 403 as an error, never retries a mismatched channel |
| T-04-03 | Tampering | Pusher envelope parsing | mitigate | `serde_json` into typed structs; non-string `data` in parse_print_job returns Err rather than mis-parsing (V5 input validation) |
| T-04-SC | Tampering | Cargo installs (futures-util, rand) | mitigate | Both audited [OK] by slopcheck in 04-RESEARCH.md Package Legitimacy Audit; resolved lockfile versions pinned |
</threat_model>

<verification>
- `cargo build --lib` exits 0 (deps resolve, config persistence compiles)
- `cargo test --lib -- pusher::` — protocol + backoff inline tests green
- `cargo test --test pusher_auth_test` — HTTP contract tests green
- `cargo test` — full suite still green (no regressions in existing tests)
</verification>

<success_criteria>
- `futures-util` and `rand` are explicit `[dependencies]`
- Activation save persists `pusher_key` + `pusher_cluster` to ConfigStore (D-01)
- `pusher_auth()` exists with `channel_name` form param, tested for 200/403/transport
- `parse_print_job` double-decodes; `extract_socket_id` parses connection_established
- `backoff_delay` respects the 60s cap under jitter
- `pusher` module registered in lib.rs with PrintEvent/PusherConfig exported
</success_criteria>

<output>
Create `.planning/phases/04-pusher-event-stream/04-01-SUMMARY.md` when done
</output>
