---
phase: 05-job-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/noren_client.rs
  - tests/print_worker_test.rs
  - src/print_worker.rs
autonomous: true
requirements: [PRT-01, PRT-08]
must_haves:
  truths:
    - "fetch_job_bytes returns the base64-decoded ESC/POS bytes from a 200 {\"bytes\":\"<b64>\"} response"
    - "fetch_job_bytes returns Err on a non-200 status (e.g. 500, 404)"
    - "ack_job returns Ok(()) on both 200 and 409 (idempotent)"
    - "ack_job returns Err on an unexpected status (e.g. 500)"
    - "agent_token is passed via bearer_auth and never appears in any log or error string"
  artifacts:
    - path: "Cargo.toml"
      provides: "explicit base64 = \"0.22\" direct dependency"
      contains: "base64"
    - path: "src/noren_client.rs"
      provides: "fetch_job_bytes() and ack_job() async HTTP functions"
      exports: ["fetch_job_bytes", "ack_job"]
    - path: "src/print_worker.rs"
      provides: "empty module skeleton so unit tests can import the crate (Wave 0)"
      min_lines: 1
    - path: "tests/print_worker_test.rs"
      provides: "mock-TCP contract tests for fetch_job_bytes and ack_job"
      contains: "spawn_stub"
  key_links:
    - from: "src/noren_client.rs::fetch_job_bytes"
      to: "base64::engine::general_purpose::STANDARD"
      via: "decode(&body.bytes)"
      pattern: "STANDARD\\s*\\.decode"
    - from: "src/noren_client.rs::ack_job"
      to: "reqwest bearer_auth"
      via: ".bearer_auth(agent_token)"
      pattern: "bearer_auth"
---

<objective>
Add the two authenticated HTTP functions Phase 5 needs — `fetch_job_bytes()` (GET ESC/POS
bytes, base64-decoded) and `ack_job()` (idempotent POST ack) — to `src/noren_client.rs`,
add the `base64 = "0.22"` direct dependency, and lay the Wave-0 test scaffold
(`tests/print_worker_test.rs`) plus an empty `src/print_worker.rs` skeleton so the tests
compile.

Purpose: These are the tested request-response primitives the print worker (Plan 02)
consumes. Building and testing them first means Plan 02 wires against a proven contract
instead of exploring HTTP shapes.
Output: `fetch_job_bytes`, `ack_job`, `base64` dep, contract tests, module skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-job-pipeline/05-CONTEXT.md
@.planning/phases/05-job-pipeline/05-RESEARCH.md
@.planning/phases/05-job-pipeline/05-PATTERNS.md

<interfaces>
<!-- Extracted from codebase. Executor should use these directly — no exploration needed. -->

From src/pusher/protocol.rs (the event this pipeline processes):
```rust
#[derive(serde::Deserialize, Debug, Clone)]
pub struct PrintEvent {
    #[serde(rename = "jobId")]
    pub job_id: String,
    #[serde(rename = "type")]
    pub job_type: String,
}
```

From src/noren_client.rs (analog to replicate — pusher_auth() at lines 186–221):
- `pub fn noren_base_url() -> String` (line 26)
- `pub async fn pusher_auth(...) -> anyhow::Result<PusherAuthResponse>` (line 186)
- Uses `anyhow::Context as _`, `.bearer_auth()`, `.send().await`, `.json().await`,
  `match resp.status().as_u16() { ... anyhow::bail! }`.

From tests/noren_client_test.rs (the mock-TCP stub to reuse — lines 15–49):
- `use tokio::net::TcpListener;`
- `async fn spawn_stub(status: u16, body: &'static str) -> String` — binds `127.0.0.1:0`,
  serves ONE request, returns `http://127.0.0.1:{port}`. Copy verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add base64 dep + empty print_worker skeleton + Wave-0 test scaffold</name>
  <files>Cargo.toml, src/print_worker.rs, src/lib.rs, tests/print_worker_test.rs</files>
  <read_first>
    - Cargo.toml (see current [dependencies] block; confirm base64 not already a direct dep)
    - src/lib.rs (lines 7–23 — the `pub mod` block; `pub mod pusher` is at line 16)
    - tests/noren_client_test.rs (lines 15–49 — copy the `spawn_stub` helper and imports verbatim)
    - .planning/phases/05-job-pipeline/05-PATTERNS.md (the `tests/print_worker_test.rs` and `spawn_stub` sections)
  </read_first>
  <action>
    Add `base64 = "0.22"` to the `[dependencies]` section of Cargo.toml (NOT under
    `[target.'cfg(windows)'.dependencies]` — base64 is portable, per D-05). It is already in
    Cargo.lock as a transitive dep (0.22.1), so this only makes the direct dependency explicit.

    Create `src/print_worker.rs` as an EMPTY module skeleton for now: a file-level doc comment
    (`//! Print worker — Phase 5 job pipeline. Implemented in Plan 02.`) and nothing else. This
    exists only so the crate compiles and tests can import `brevly_print::noren_client`. Plan 02
    fills in `run_print_worker()`.

    In src/lib.rs, add `pub mod print_worker;` immediately after `pub mod pusher;` (line 16).

    Create `tests/print_worker_test.rs` with the Wave-0 test scaffold. Copy the `spawn_stub`
    helper and its imports (`use tokio::io::AsyncWriteExt;`, `use tokio::net::TcpListener;`)
    verbatim from tests/noren_client_test.rs. Also add `use base64::Engine as _;` and
    `use brevly_print::noren_client::{fetch_job_bytes, ack_job};`. Write three `#[tokio::test]`
    functions with these exact names and assertions (fetch_job_bytes/ack_job do not exist yet —
    these are RED until Task 2 lands them; that is expected for Wave-0 scaffold):
      - `test_fetch_job_bytes_200_decodes_base64`: encode `raw = b"\x1b\x40Hello\x1d\x56\x00"`
        via `STANDARD.encode(raw)`, build body `{"bytes":"<encoded>"}`, `Box::leak` it to
        `&'static str`, `spawn_stub(200, body)`, call
        `fetch_job_bytes(&reqwest::Client::new(), &base_url, "tok-test", "job-001").await`,
        assert result equals `raw`.
      - `test_fetch_job_bytes_non_200_returns_err`: `spawn_stub(500, r#"{"error":"server error"}"#)`,
        call fetch_job_bytes, assert `result.is_err()`.
      - `test_ack_job_409_returns_ok`: `spawn_stub(409, r#"{"error":"already acked"}"#)`, call
        `ack_job(&reqwest::Client::new(), &base_url, "tok-test", "job-001").await`, assert
        `result.is_ok()` with message "409 must be Ok(()) — idempotent by design (C4)".
      - `test_ack_job_500_returns_err`: `spawn_stub(500, r#"{"error":"boom"}"#)`, call ack_job,
        assert `result.is_err()`.
  </action>
  <verify>
    <automated>test -f src/print_worker.rs &amp;&amp; grep -q '^base64 = "0.22"' Cargo.toml &amp;&amp; grep -q "pub mod print_worker;" src/lib.rs &amp;&amp; grep -q "spawn_stub" tests/print_worker_test.rs</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` [dependencies] section contains a line matching `base64 = "0.22"` (not window-gated)
    - `src/print_worker.rs` exists (at minimum a doc comment)
    - `src/lib.rs` contains `pub mod print_worker;` on its own line after `pub mod pusher;`
    - `tests/print_worker_test.rs` defines `spawn_stub` and the four test fns named above
    - `tests/print_worker_test.rs` contains `use brevly_print::noren_client::{fetch_job_bytes, ack_job};`
    - The token string `"tok-test"` appears only as a function argument, never inside an assertion-format or eprintln
  </acceptance_criteria>
  <done>base64 dep added, print_worker.rs skeleton + lib.rs mod present, four contract tests scaffolded referencing fetch_job_bytes/ack_job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetch_job_bytes() and ack_job() in noren_client.rs</name>
  <files>src/noren_client.rs</files>
  <behavior>
    - fetch_job_bytes on 200 with `{"bytes":"<base64>"}` returns the decoded `Vec<u8>` (test_fetch_job_bytes_200_decodes_base64 passes)
    - fetch_job_bytes on 500 returns Err (test_fetch_job_bytes_non_200_returns_err passes)
    - ack_job on 409 returns Ok(()) (test_ack_job_409_returns_ok passes)
    - ack_job on 500 returns Err (test_ack_job_500_returns_err passes)
  </behavior>
  <read_first>
    - src/noren_client.rs (read pusher_auth() lines 186–221 in full — replicate its structure: url format, .bearer_auth, .send().await, .context(), status match, anyhow::bail!)
    - .planning/phases/05-job-pipeline/05-PATTERNS.md (the fetch_job_bytes and ack_job code patterns, lines 159–249)
    - .planning/phases/05-job-pipeline/05-CONTEXT.md (D-04 exact signatures; Specifics section for response shape)
  </read_first>
  <action>
    Add two async functions at the bottom of src/noren_client.rs, mirroring the `pusher_auth()`
    structure exactly (same `anyhow::Context as _` import already present at top; add
    `use base64::Engine as _;` — either at top or inside the function scope).

    Function 1 — exact signature:
    `pub async fn fetch_job_bytes(client: &reqwest::Client, base_url: &str, agent_token: &str, job_id: &str) -> anyhow::Result<Vec<u8>>`
    Body: define a local `#[derive(serde::Deserialize)] struct BytesResponse { bytes: String }`.
    Build `url = format!("{base_url}/api/agent/jobs/{job_id}/bytes")`. Call
    `client.get(&url).bearer_auth(agent_token).send().await` with
    `.context("fetch_job_bytes: HTTP transport error")?`. Then
    `match resp.status().as_u16()`: on `200` do
    `let body: BytesResponse = resp.json().await.context("fetch_job_bytes: response parse error")?;`
    then return
    `base64::engine::general_purpose::STANDARD.decode(&body.bytes).context("fetch_job_bytes: base64 decode error")`;
    any other status => `anyhow::bail!("fetch_job_bytes: unexpected status {status}")`.

    Function 2 — exact signature:
    `pub async fn ack_job(client: &reqwest::Client, base_url: &str, agent_token: &str, job_id: &str) -> anyhow::Result<()>`
    Body: `url = format!("{base_url}/api/agent/jobs/{job_id}/ack")`. Call
    `client.post(&url).bearer_auth(agent_token).send().await` with
    `.context("ack_job: HTTP transport error")?` (POST with no body — reqwest sends none by
    default). Then `match resp.status().as_u16() { 200 | 409 => Ok(()), status => anyhow::bail!("ack_job: unexpected status {status}") }`.
    409 MUST map to `Ok(())` — idempotent by design (C4 / D-04): a post-crash ack repeat is normal.

    T-02-02 constraint: `agent_token` is passed ONLY through `.bearer_auth(agent_token)`. It must
    never be interpolated into any `format!`, `eprintln!`, `.context()`, or `anyhow::bail!` string.
  </action>
  <verify>
    <automated>cargo test -q --test print_worker_test fetch_job_bytes ack_job 2>&amp;1 | tail -5; cargo test -q --test print_worker_test</automated>
  </verify>
  <acceptance_criteria>
    - `src/noren_client.rs` contains `pub async fn fetch_job_bytes(` with return type `anyhow::Result<Vec<u8>>`
    - `src/noren_client.rs` contains `pub async fn ack_job(` with return type `anyhow::Result<()>`
    - `src/noren_client.rs` contains `200 | 409 => Ok(())` (the idempotent ack mapping)
    - `src/noren_client.rs` contains `STANDARD` and `.decode(` (Engine API, not the removed 0.21 free functions)
    - `cargo test -q --test print_worker_test` exits 0 with all four tests passing
    - No occurrence of `agent_token` inside any string literal in the two new functions
  </acceptance_criteria>
  <done>Both HTTP functions implemented; all four Wave-0 contract tests green; 409 mapped to Ok(()); base64 Engine API used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → Noren HTTP | Agent sends Bearer token; receives untrusted JSON (base64 bytes) across the network |
| Noren response → base64 decode | Untrusted string decoded into `Vec<u8>` before reaching the printer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `agent_token` in `fetch_job_bytes`/`ack_job` | mitigate | Token passed only via `.bearer_auth()`; never interpolated into any format/log/error string (T-02-02). Acceptance criterion asserts absence. Severity: high — mitigated. |
| T-05-02 | Tampering | base64 body from Noren response (`V5 Input Validation`) | mitigate | Typed `serde` deserialization + `base64::STANDARD.decode()` returns `Result`; malformed input becomes an `Err` (logged), never a panic. Severity: low. |
| T-05-03 | Denial of Service | 409 ack response treated as error → retry storm | mitigate | D-04: 409 maps to `Ok(())`; no spurious retry. Severity: low. |
| T-05-SC | Tampering | `base64 = "0.22"` install | accept | Already a vetted transitive dep in Cargo.lock (0.22.1); `[ASSUMED]` per RESEARCH audit (slopcheck unavailable). Making it a direct dep introduces no new compiled code. No blocking checkpoint required. Severity: low. |
</threat_model>

<verification>
- `cargo build -q` succeeds (base64 dep resolves)
- `cargo test -q --test print_worker_test` — all four contract tests pass
- `cargo clippy --all-targets -q` produces no new warnings on the two new functions
</verification>

<success_criteria>
- `fetch_job_bytes()` and `ack_job()` exist in `src/noren_client.rs` with the D-04 signatures
- `base64 = "0.22"` is a direct dependency
- All four Wave-0 contract tests are green
- Token never logged; 409 ack is Ok(()); base64 Engine API used
</success_criteria>

<output>
Create `.planning/phases/05-job-pipeline/05-01-SUMMARY.md` when done
</output>
