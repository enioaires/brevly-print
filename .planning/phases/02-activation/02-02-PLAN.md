---
phase: 02-activation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/printer/mod.rs
  - src/printer/spooler.rs
  - src/printer/serial.rs
autonomous: true
requirements: [ACT-04, ACT-05]

must_haves:
  truths:
    - "On Windows, enumerate_printers() returns a combined list of Windows spooler printers labelled '(USB)' and COM ports labelled '(Serial)', with the default printer flagged"
    - "WindowsSpoolerPrinter.print_raw() submits bytes to the spooler with pDatatype = \"RAW\" (C1)"
    - "SerialPrinter.print_raw() writes bytes to the selected COM port"
    - "printer_from_entry() maps PrinterId::Spooler → WindowsSpoolerPrinter and PrinterId::Serial → SerialPrinter"
    - "Linux build/test remains green (these files are cfg(windows) only)"
  artifacts:
    - path: "src/printer/spooler.rs"
      provides: "WindowsSpoolerPrinter (WritePrinter RAW path)"
      contains: "RAW"
    - path: "src/printer/serial.rs"
      provides: "SerialPrinter (serialport write)"
      contains: "impl Printer for SerialPrinter"
    - path: "src/printer/mod.rs"
      provides: "windows_enumerate_printers() + printer_from_entry() Windows branches filled"
      contains: "get_printers"
  key_links:
    - from: "src/printer/spooler.rs"
      to: "windows::Win32::Graphics::Printing"
      via: "OpenPrinterW/StartDocPrinterW(RAW)/WritePrinter/EndDocPrinter"
      pattern: "WritePrinter"
    - from: "src/printer/mod.rs"
      to: "printers::get_printers + serialport::available_ports"
      via: "combined enumeration"
      pattern: "available_ports"
---

<objective>
Fill the Windows hardware side of the `Printer` trait created in Plan 01: real combined printer
enumeration (Windows spooler printers + COM ports), the WritePrinter RAW spooler path, and the
serialport write path. Completes ACT-04 (real combined dropdown data) and the Windows half of
ACT-05 (bytes actually reach the hardware). Pitfall C1 — pDatatype MUST be "RAW" — is the load-bearing
detail; the test-print (Plan 03 / Windows manual checklist) is its validation gate.

Purpose: the activation UI (Plan 03) and Phase 5 job pipeline both depend on a working `Printer`
impl. This plan is 100% `#[cfg(windows)]`; the Linux build is unaffected.
Output: `printer/spooler.rs`, `printer/serial.rs`, filled Windows branches in `printer/mod.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-activation/02-RESEARCH.md
@.planning/phases/02-activation/02-PATTERNS.md
@.planning/phases/02-activation/02-01-SUMMARY.md

<interfaces>
<!-- Contracts defined by Plan 01 that this plan implements against. -->

From src/printer/mod.rs (Plan 01):
```
pub struct PrinterEntry { pub display_name: String, pub id: PrinterId, pub is_default: bool }
pub enum PrinterId { Spooler(String), Serial(String) }
pub trait Printer { fn print_raw(&self, bytes: &[u8]) -> Result<(), PrinterError>; }
#[cfg(windows)] fn windows_enumerate_printers() -> Vec<PrinterEntry>  // todo!() placeholder to fill
pub fn printer_from_entry(id: &PrinterId) -> Box<dyn Printer>          // Windows branch todo!() to fill
```

From src/printer/error.rs (Plan 01):
```
pub enum PrinterError { NotFound(String), PrintFailed(String), SerialPort(String), Io(#[from] std::io::Error) }
```

Cargo.toml windows-only deps already present: windows 0.62 (features Win32_Graphics_Printing, Win32_Foundation), printers 2, serialport 4.9.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: spooler.rs — WritePrinter RAW path (C1) + serial.rs — serialport write</name>
  <files>src/printer/spooler.rs, src/printer/serial.rs</files>
  <read_first>
    - .planning/phases/02-activation/02-RESEARCH.md (Pattern 3 WritePrinter RAW full sequence; Pitfall 1/C1; Pitfall 5 exact printer name; windows crate API note on StartDocPrinterW/ClosePrinter)
    - .planning/phases/02-activation/02-PATTERNS.md (§ src/printer/spooler.rs, § src/printer/serial.rs)
    - src/credential_store/dpapi.rs (Windows-only impl file structure: #![cfg(windows)] header + struct + impl)
    - src/printer/error.rs (PrinterError variants to return)
  </read_first>
  <action>
    Create `src/printer/spooler.rs` under `#![cfg(windows)]` (file-level gate, per dpapi.rs precedent). Define `pub struct WindowsSpoolerPrinter { pub printer_name: String }` and `impl Printer for WindowsSpoolerPrinter` whose `print_raw` delegates to an `unsafe fn write_raw_to_spooler(printer_name: &str, data: &[u8]) -> Result<(), PrinterError>` implementing the RESEARCH Pattern 3 Win32 sequence: OpenPrinterW → StartDocPrinterW (level 1, `DOC_INFO_1W` with `pDatatype` set to a UTF-16 `"RAW\0"` literal — C1, annotate the line `// CRITICAL C1: RAW or ESC/POS becomes silent garbage`) → StartPagePrinterW → WritePrinter → EndPagePrinterW → EndDocPrinterW → ClosePrinter. Map Win32 failures to `PrinterError::PrintFailed(...)` / `NotFound(...)` (OpenPrinterW error 1801 → NotFound). Confirm the exact windows-0.62 names for StartDocPrinterW (takes `*const DOC_INFO_1W`, level `1`) and the close fn (`ClosePrinter` vs `ClosePrinterW`) against the windows-docs-rs `Win32::Graphics::Printing` module before finalizing — do NOT transform the printer name (Pitfall 5, pass `printer_name` verbatim). Create `src/printer/serial.rs` under `#![cfg(windows)]` with `pub struct SerialPrinter { pub port_name: String }` and `impl Printer for SerialPrinter` using `serialport::new(&self.port_name, 9600).open()` then `std::io::Write::write_all(bytes)`, mapping open errors to `PrinterError::SerialPort(...)` and write errors to `PrinterError::Io`.
  </action>
  <verify>
    <automated>cargo build --target x86_64-pc-windows-msvc 2>/dev/null || echo "MISSING — Windows target not installed on Linux CI; source-assertion gate below applies. Verified on windows-msvc CI."</automated>
  </verify>
  <acceptance_criteria>
    - src/printer/spooler.rs passes a `DOC_INFO_1W` whose `pDatatype` is a UTF-16 `"RAW"` literal (grep: `grep -v '^\s*//' src/printer/spooler.rs | grep -c 'RAW'` ≥ 1) and the line is annotated as the C1 critical detail.
    - spooler.rs calls WritePrinter and EndDocPrinter (grep both symbols present).
    - src/printer/spooler.rs and serial.rs both begin with `#![cfg(windows)]`.
    - serial.rs uses `serialport::new(...).open()` and `write_all`.
    - Linux `cargo build` still succeeds (these files do not compile on Linux; no non-Windows references leak out).
  </acceptance_criteria>
  <done>Both Windows print paths implemented; C1 RAW datatype baked in and annotated; Linux build unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Fill windows_enumerate_printers() + printer_from_entry() Windows branches</name>
  <files>src/printer/mod.rs</files>
  <read_first>
    - .planning/phases/02-activation/02-RESEARCH.md (Pattern 7 combined enumeration; note on printers::Printer.name field; note on serialport SerialPortInfo.port_name)
    - .planning/phases/02-activation/02-PATTERNS.md (§ src/printer/mod.rs enumerate function)
    - .planning/phases/02-activation/02-UI-SPEC.md (Printer ComboBox item format: "{name} (USB)" / "{COM} (Serial)")
    - src/printer/mod.rs (Plan 01 placeholders to replace)
  </read_first>
  <action>
    Replace the Plan 01 `todo!()` in `windows_enumerate_printers()` with the Pattern 7 body: call `printers::get_default_printer()` for the default name, iterate `printers::get_printers()` pushing `PrinterEntry { display_name: format!("{} (USB)", p.name), id: PrinterId::Spooler(p.name.clone()), is_default: default==name }`, then iterate `serialport::available_ports()` pushing `PrinterEntry { display_name: format!("{} (Serial)", port.port_name), id: PrinterId::Serial(port.port_name.clone()), is_default: false }`. Use `p.name` verbatim (Pitfall 5). Replace the Windows branch of `printer_from_entry()` to match: `PrinterId::Spooler(name) => Box::new(spooler::WindowsSpoolerPrinter { printer_name: name.clone() })`, `PrinterId::Serial(port) => Box::new(serial::SerialPrinter { port_name: port.clone() })`. Keep the `#[cfg(not(windows))]` branches (empty Vec / StubPrinter) exactly as Plan 01 left them.
  </action>
  <verify>
    <automated>cargo build --target x86_64-pc-windows-msvc 2>/dev/null || echo "MISSING — verified on windows-msvc CI; Linux source-assertion gate applies."</automated>
  </verify>
  <acceptance_criteria>
    - src/printer/mod.rs Windows enumeration uses `get_printers` and `available_ports` (both symbols grep-present) and formats display names with the trailing `(USB)` / `(Serial)` labels per UI-SPEC.
    - No `todo!()` remains in printer/mod.rs Windows branches.
    - printer_from_entry() constructs WindowsSpoolerPrinter and SerialPrinter for the respective PrinterId variants.
    - `cargo test` (Linux) still green — the `#[cfg(not(windows))]` path unchanged (enumerate returns empty).
  </acceptance_criteria>
  <done>Combined Windows enumeration + factory implemented against Plan 01 contracts; Linux path untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → printer hardware | Raw ESC/POS bytes cross into a USB spooler / COM port. Local, physical; not network-exposed. |
| Win32 unsafe FFI | `unsafe` block calling winspool; handle lifetime + pointer correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | ESC/POS bytes misinterpreted (missing RAW datatype) | mitigate | pDatatype="RAW" enforced (C1) and asserted in acceptance criteria; Windows manual test-print is the runtime gate. |
| T-02-06 | Denial of Service | leaked printer handle on error path | mitigate | Ensure ClosePrinter runs on every exit path of write_raw_to_spooler (early-return errors still close the handle). |
| T-02-07 | Elevation of Privilege | unsafe Win32 FFI (pointer/length mismatch) | accept | Bytes length passed to WritePrinter is `data.len()`; no attacker-controlled length; local single-user agent. |
</threat_model>

<verification>
- Linux `cargo build` + `cargo test` remain green (files are cfg(windows) only).
- Windows-msvc build compiles (CI or owner box) — see Windows Manual Checklist in 02-VALIDATION.md.
- Manual (Windows hardware, phase gate): test-print produces a legible coupon + paper cut (validates C1 RAW).
</verification>

<success_criteria>
- Combined enumeration returns spooler + COM entries with correct "(USB)"/"(Serial)" labels and default flagged.
- Spooler path uses RAW datatype; serial path writes to COM.
- printer_from_entry() dispatches correctly.
- No Linux regression.
</success_criteria>

<output>
Create `.planning/phases/02-activation/02-02-SUMMARY.md` when done.
</output>
