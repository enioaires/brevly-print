# Phase 2: Activation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-15
**Phase:** 2-activation
**Areas discussed:** Serial + validation + 409 policy, Printer selection + test-print, Re-activation (DPAPI loss), Offline / backend unreachable, Window layout, Empty printer state, Autostart timing, Branding

---

## Serial + validation + 409 policy

| Option | Description | Selected |
|--------|-------------|----------|
| Segmented/masked field | XXXX-XXXX-XXXX auto-mask; needs fixed serial format | |
| Freeform text field | Simple input, accepts paste; flexible if format varies | |
| You decide | Planner picks per real Noren serial format | ✓ |

**User's choice (serial field):** You decide.
**Notes:** Serial format is generated by Noren; pin it before implementing.

| Option | Description | Selected |
|--------|-------------|----------|
| Re-bind with confirmation | "Migrar licença para esta máquina?" → Noren invalidates old token | (fallback) |
| Block + contact support | Hard error, no proceed | |
| Auto re-bind | Silent migration | |

**User's choice (409 policy):** Free-text — "acho que o serial vale pro tenant não? pode vincular
várias impressoras se necessário, mas se isso for mudar muito nossa ideia pode ser a opção 1."
This surfaced a licensing-model question (per-tenant vs per-machine serial), asked as a follow-up.

**Follow-up (serial model):** User chose **"mantém o que tava planejado pra não atrapalhar o que
está sendo feito no Noren"** → serial-per-machine (current contract), 409 → re-bind with
confirmation (option 1).

| Option | Description | Selected |
|--------|-------------|----------|
| Spinner on button + inline error | Button spins; error in red below field | ✓ |
| Simple status message | One status line, button unchanged | |
| You decide | Planner picks | |

**User's choice (feedback):** Spinner on button + inline error.

---

## Printer selection + test-print

| Option | Description | Selected |
|--------|-------------|----------|
| Single list with type label | `EPSON (USB)` / `COM3 (Serial)` in one dropdown | ✓ |
| Grouped by type | Sections for USB/Windows vs COM | |
| You decide | Planner picks | |

**User's choice (list):** Single list with type label.

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-select Windows default | `get_default_printer` pre-chosen | ✓ |
| Force explicit choice | Starts on "Selecione..." | |

**User's choice (pre-selection):** Pre-select the Windows default.

| Option | Description | Selected |
|--------|-------------|----------|
| Visible receipt + mandatory | Legible coupon + cut; owner confirms before save | ✓ |
| Visible receipt + optional | Prints coupon, save doesn't require it | |
| Cut bytes only | ESC @ + GS V, no visible text | |

**User's choice (test-print):** Visible receipt + mandatory.

| Option | Description | Selected |
|--------|-------------|----------|
| Warn but allow save | Shows error, still lets you save (Phase 6 retry) | ✓ |
| Block save until pass | Can't complete until test prints | |
| You decide | Planner picks | |

**User's choice (test failure):** Warn but allow save. Reconciled with "mandatory": testing is a
required step, but its success is not a save gate.

---

## Re-activation (DPAPI loss)

| Option | Description | Selected |
|--------|-------------|----------|
| Short message + reactivation | Banner explaining license still valid | |
| Silent (reopens like first time) | No explanation | |
| You decide | Planner picks copy/behavior | ✓ |

**User's choice (detection UX):** You decide (guide: non-technical owner → lean reassuring message).

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-fill serial + printer | Store serial in SQLite; both pre-filled | |
| Pre-fill printer only | Printer survives, serial re-typed | |
| All blank | Fresh activation, nothing pre-filled | ✓ |

**User's choice (pre-fill):** All blank — do not persist serial to disk.

---

## Offline / backend unreachable

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct clear error + manual retry | Network vs invalid-serial worded differently | ✓ |
| Generic error + retry | One message for any failure | |
| You decide | Planner maps errors | |

**User's choice (offline):** Distinct clear error + manual retry.

| Option | Description | Selected |
|--------|-------------|----------|
| Close window + exit process | Save, register autostart, exit 0 | (recommended) |
| Close + keep running headless | Stays alive without tray | |
| You decide | Planner picks per Phase 2/3 boundary | ✓ |

**User's choice (post-save):** You decide (recommended default recorded: close + exit).

---

## Window layout

| Option | Description | Selected |
|--------|-------------|----------|
| Single screen | All fields on one screen | ✓ |
| Wizard steps | serial → printer → test → done | |
| You decide | Planner picks | |

**User's choice:** Single screen.

---

## Empty printer state

| Option | Description | Selected |
|--------|-------------|----------|
| Instruction + "Atualizar lista" button | Re-enumerate in place; save disabled until found | ✓ |
| Static message only | No refresh button | |
| You decide | Planner picks | |

**User's choice:** Instruction + refresh button; save disabled until a printer exists.

---

## Autostart timing

| Option | Description | Selected |
|--------|-------------|----------|
| At save time | Register on activation save; failure warns but completes | ✓ |
| After first runtime run | Only after Phase 3 runtime succeeds once | |
| You decide | Planner picks | |

**User's choice:** Register autostart at save time.

---

## Branding

| Option | Description | Selected |
|--------|-------------|----------|
| Delegate to /gsd:ui-phase 2 | Dedicated UI-SPEC.md for logo/colors/icon/language | ✓ |
| Minimal now, no ui-phase | Plain egui, title only | |
| You decide | Conditional | |

**User's choice:** Delegate to `/gsd:ui-phase 2`.

---

## Claude's Discretion

- Serial field style (segmented vs freeform) — pin to real Noren serial format.
- Re-activation detection banner copy/behavior — lean reassuring for non-technical owner.
- Post-save lifecycle — recommended default: close window + exit process.
- `machineId` generation/stability — technical, planner decides.
- Window sizing / egui widget styling (branding proper → ui-phase 2).

## Deferred Ideas

- Per-tenant / multi-machine serial licensing model (kept per-machine to match current Noren contract).
- One agent driving multiple printers (out of scope v1; agent = one printer).
- Branding / visual identity → `/gsd:ui-phase 2`.
- Tray/runtime/signed installer → Phase 3; print retry + toast → Phase 6.
