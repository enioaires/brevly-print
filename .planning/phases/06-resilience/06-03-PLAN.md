---
phase: 06-resilience
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/retry_task.rs
  - src/lib.rs
  - src/health_state.rs
  - src/main.rs
autonomous: true
requirements: [RES-01, RES-02, RES-04]
must_haves:
  truths:
    - "A fourth Tokio task retries failed prints 3× at 30s intervals, then gives up"
    - "On retry exhaustion the tray turns red (send_health(Problem)) and a Windows toast fires"
    - "On boot, jobs left at status='printing' with no retry_queue row are re-fetched and re-queued"
    - "On retry success the tray returns to green (send_health(Connected))"
  artifacts:
    - path: "src/retry_task.rs"
      provides: "run_retry_task: crash recovery + 5s poll loop + retry/exhaust state machine"
      exports: ["run_retry_task"]
      min_lines: 80
    - path: "src/health_state.rs"
      provides: "Problem strings updated to printer-failure wording (D-10)"
      contains: "Falha na impressora"
    - path: "src/main.rs"
      provides: "retry task spawn + second Box<dyn Printer> construction + health proxy"
      contains: "run_retry_task"
  key_links:
    - from: "src/main.rs"
      to: "retry_task::run_retry_task"
      via: "rt_handle.spawn in the is_runtime block"
      pattern: "run_retry_task"
    - from: "src/retry_task.rs"
      to: "printer.print_raw + ack_job"
      via: "poll loop retry attempt"
      pattern: "print_raw"
    - from: "src/retry_task.rs"
      to: "send_health(HealthState::Problem)"
      via: "retry exhaustion (attempt_count >= 3)"
      pattern: "HealthState::Problem"
---

<objective>
Complete the local-failure resilience path (RES-01 retry 3×/30s, RES-02 toast + red tray on
exhaustion, RES-04 boot crash recovery). This adds the fourth Tokio task `run_retry_task` and
wires it into the Runtime block, updates the tray's Problem wording to reflect printer failure,
and declares the new module.

Purpose: 06-02 made failed prints RECOVERABLE (they land in retry_queue at status='printing').
This plan makes them ACTUALLY RETRIED and, after 3 failures, alerts the operator. It also
drains orphaned 'printing' rows at startup so a crash mid-print never loses a ticket.
Output: `src/retry_task.rs` (new), `src/lib.rs` (module decl), `src/health_state.rs` (strings),
`src/main.rs` (spawn + printer + proxy). Turns the Wave-0 retry_exhaustion / retry_task /
crash_recovery stubs GREEN.

This plan does NOT touch the Pusher pending-pull path (that is 06-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resilience/06-CONTEXT.md
@.planning/phases/06-resilience/06-PATTERNS.md
@.planning/phases/06-resilience/06-RESEARCH.md

<interfaces>
<!-- Extracted verbatim from the codebase — build against these, do not re-explore. -->

run_print_worker WAL-connection open pattern (src/print_worker.rs lines 39–51) — COPY into retry_task:
  `rusqlite::Connection::open(&db_path)` → on Err eprintln + return;
  `conn.pragma_update(None, "journal_mode", "WAL")` → on Err eprintln + return.
  ADD after WAL enable (Pitfall 2): `let _ = conn.busy_timeout(std::time::Duration::from_secs(5));`

noren_client functions available (src/noren_client.rs):
  `pub async fn fetch_job_bytes(client, base_url, agent_token, job_id) -> anyhow::Result<Vec<u8>>`
  `pub async fn ack_job(client, base_url, agent_token, job_id) -> anyhow::Result<()>` (200/204/409 = Ok)
  `fn validate_job_id(job_id) -> anyhow::Result<()>` — currently PRIVATE; retry task does NOT need it
    directly (job_ids come from local DB, already validated on ingest) — do not call it here.

Printer trait (src/printer/mod.rs): `pub trait Printer: Send { fn print_raw(&self, bytes: &[u8]) -> Result<(), PrinterError>; }`
  `pub fn printer_from_entry(id: &PrinterId) -> Box<dyn Printer>` — Box is already Send (Printer: Send).
  `pub enum PrinterId { Spooler(String), Serial(String) }`

HealthState (src/health_state.rs): `pub enum HealthState { Connected, Reconnecting, Problem }`.
  send_health closure type (matches run_pusher_loop): `impl Fn(HealthState) + Send + 'static`.

main.rs Runtime block (lines 403–466), values in scope BEFORE the pusher/worker spawns:
  `conn` (main App SQLite conn), `db_path`, `http` (reqwest::Client), `agent_token` (String, moved into
  pusher spawn), `worker_token` (clone of agent_token), `worker_base_url` (clone of auth_url = Noren base
  URL), `event_loop` (for create_proxy()), `rt_handle`.
  Existing pusher proxy pattern (lines 446–449):
    `let proxy_for_pusher = event_loop.create_proxy();`
    `let send_health = move |state: HealthState| { let _ = proxy_for_pusher.send_event(UserEvent::HealthChanged(state)); };`
  Existing printer_id construction (mirrors print_worker.rs lines 64–84): read `printer_name` +
  `printer_type` via `config_store::get(&conn, ...)`, then `PrinterId::Serial(name)` if
  printer_type=="serial" else `PrinterId::Spooler(name)`, then `printer_from_entry(&id)`.
  UserEvent::HealthChanged already exists (used by pusher). rt_handle.spawn requires F: Future + Send.

Toast (D-07, #[cfg(windows)] — Cargo.toml has tauri-winrt-notification 0.8):
  `use tauri_winrt_notification::Toast; let _ = Toast::new(Toast::POWERSHELL_APP_ID).title(..).text1(..).text2(..).show();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update health_state.rs Problem strings + declare retry_task module (D-10)</name>
  <files>src/health_state.rs, src/lib.rs</files>
  <read_first>
    - src/health_state.rs (tooltip() Problem arm line 26; status_label() Problem arm line 35; existing tests lines 57–84)
    - src/lib.rs (module declarations lines 7–17; add `pub mod retry_task;` after `pub mod print_worker;`)
    - .planning/phases/06-resilience/06-CONTEXT.md (D-10 exact before/after strings)
  </read_first>
  <action>
    In `src/health_state.rs`:
    - `tooltip()` Problem arm (line 26): change `"Brevly Print — Problema de conexão"` to
      `"Brevly Print — Falha na impressora"`.
    - `status_label()` Problem arm (line 35): change `"Problema de conexão"` to `"Falha na impressora"`.
    - Update the doc comment on the Problem variant (line 16) if it references "connection failed" —
      reword to reflect that in Phase 6, Problem is exclusively a printer failure.
    - Do NOT touch the Connected/Reconnecting arms, the icon() impl, or the tests (the existing
      `all_states_have_distinct_tooltips` and `status_labels_are_non_empty` tests still pass — the new
      strings remain distinct and non-empty).

    In `src/lib.rs`: add `pub mod retry_task;` immediately after the existing `pub mod print_worker;`
    line. This must be present before main.rs can reference `crate::retry_task::run_retry_task`.
    (retry_task.rs is created in Task 2 — declaring the module before the file exists will fail to
    compile, so ensure Task 2 runs together with this task or create an empty retry_task.rs stub first
    if executing Task 1 in isolation.)
  </action>
  <verify>
    <automated>cargo test health_state</automated>
  </verify>
  <acceptance_criteria>
    - `src/health_state.rs` tooltip() Problem arm returns `"Brevly Print — Falha na impressora"`
    - `src/health_state.rs` status_label() Problem arm returns `"Falha na impressora"`
    - `src/lib.rs` contains `pub mod retry_task;`
    - `cargo test health_state` exits 0 (distinctness + non-empty tests still pass)
    - grep confirms `"Problema de conexão"` no longer appears in src/health_state.rs
  </acceptance_criteria>
  <done>Problem wording reflects printer failure; retry_task module declared.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/retry_task.rs — crash recovery + poll loop + retry state machine (D-03/04/05/06/07)</name>
  <files>src/retry_task.rs</files>
  <read_first>
    - src/print_worker.rs (WAL open pattern lines 39–51; conn.execute match-arm style; ack_job/fetch_job_bytes call sites)
    - src/pusher/client.rs (interval + MissedTickBehavior::Delay + burn-first-tick pattern lines 295–300; conn.prepare/query_map pattern in the tests block)
    - src/noren_client.rs (fetch_job_bytes / ack_job signatures — lines 257–328)
    - src/printer/mod.rs (Printer trait, printer_from_entry, PrinterId)
    - src/health_state.rs (HealthState variants)
    - .planning/phases/06-resilience/06-CONTEXT.md (D-03 signature, D-04 WAL conn, D-05 crash recovery, D-06 poll loop full branching, D-07 toast)
    - .planning/phases/06-resilience/06-PATTERNS.md (retry_task.rs section — full concrete SQL + snippets for every branch)
    - tests/retry_task_test.rs (the Wave-0 SQL-invariant tests + #[ignore]d exhaustion/smoke stubs this task must satisfy)
  </read_first>
  <action>
    Create `src/retry_task.rs` implementing `pub async fn run_retry_task(db_path: PathBuf,
    agent_token: String, base_url: String, http: reqwest::Client, printer: Box<dyn
    crate::printer::Printer + Send>, send_health: impl Fn(HealthState) + Send + 'static)` — exact
    signature from D-03.

    Imports: `std::path::PathBuf`, `std::time::Duration`, `tokio::time::{interval,
    MissedTickBehavior}`, and from crate: `health_state::HealthState`, `noren_client::{ack_job,
    fetch_job_bytes}`. Prefix all logs `"[brevly-print] Retry task: ..."`. Never log agent_token.

    STARTUP (before the poll loop):
    1. Open the WAL connection using the print_worker pattern (open → Err return; pragma WAL → Err
       return; then `let _ = conn.busy_timeout(Duration::from_secs(5));` per Pitfall 2).
    2. Crash recovery (D-05): prepare `SELECT job_id, job_type FROM printed_jobs WHERE status='printing'
       AND job_id NOT IN (SELECT job_id FROM retry_queue)`, collect rows via query_map into a
       `Vec<(String,String)>`. For each orphan: `fetch_job_bytes(&http, &base_url, &agent_token,
       &job_id).await` — on Ok(bytes): `INSERT OR IGNORE INTO retry_queue (job_id, job_type,
       escpos_bytes, attempt_count, next_retry_at, last_error, created_at) VALUES (?1, ?2, ?3, 1,
       datetime('now'), 'crash recovery', datetime('now'))` binding `bytes.as_slice()` (next_retry_at
       = now → immediate retry). On Err: `eprintln!("[brevly-print] Retry task: crash recovery fetch
       failed for {job_id}: {e:#}")` and skip (row stays 'printing', re-attempted next boot — Pitfall 3,
       documented acceptable).

    POLL LOOP (D-06): `let mut poll = interval(Duration::from_secs(5));
    poll.set_missed_tick_behavior(MissedTickBehavior::Delay);` then burn the first immediate tick
    with `poll.tick().await;`. Loop forever: `poll.tick().await;` then prepare `SELECT job_id,
    job_type, escpos_bytes, attempt_count FROM retry_queue WHERE next_retry_at <= datetime('now')
    ORDER BY next_retry_at ASC LIMIT 10`, collect into `Vec<(String, String, Vec<u8>, i64)>`. For
    each row:
    - Crash fence: `UPDATE printed_jobs SET status='printing' WHERE job_id=?1` (log-and-continue on
      Err — same tolerance as print_worker's fence).
    - `printer.print_raw(&escpos_bytes)`:
      - Ok(()): C4 ordering — `UPDATE printed_jobs SET status='printed', printed_at=datetime('now')
        WHERE job_id=?1` → `ack_job(&http, &base_url, &agent_token, &job_id).await` (log on Err, do not
        abort) → `DELETE FROM retry_queue WHERE job_id=?1` → `send_health(HealthState::Connected)`.
      - Err(e) where `attempt_count < 3`: `UPDATE retry_queue SET attempt_count=attempt_count+1,
        next_retry_at=datetime('now', '+30 seconds'), last_error=?2 WHERE job_id=?1` binding
        `[job_id, e.to_string()]`.
      - Err(e) otherwise (attempt_count >= 3 — EXHAUSTED): eprintln the exhaustion;
        `UPDATE printed_jobs SET status='failed', failed_at=datetime('now') WHERE job_id=?1` →
        `DELETE FROM retry_queue WHERE job_id=?1` → `send_health(HealthState::Problem)` →
        call `show_print_failure_toast()`.

    Add a private `fn show_print_failure_toast()` (D-07): inside `#[cfg(windows)] { use
    tauri_winrt_notification::Toast; let _ = Toast::new(Toast::POWERSHELL_APP_ID).title("Brevly
    Print — Falha na impressão").text1("Falha ao imprimir após 3 tentativas.").text2("Verifique se a
    impressora está ligada e com papel.").show(); }` and `#[cfg(not(windows))] eprintln!("[brevly-print]
    Retry task: print failure toast (Linux: stderr only)");`.

    Add a `#[cfg(test)] mod tests` block with at least one unit test that exercises the exhaustion
    branch WITHOUT a real printer: define a `struct AlwaysFailPrinter;` implementing `Printer` whose
    `print_raw` returns `Err(PrinterError::...)`, seed an in-memory retry_queue row with
    attempt_count=3 and a printed_jobs row, run the exhaustion SQL branch logic (either by calling a
    small extracted helper or by replicating the UPDATE/DELETE), and assert printed_jobs.status
    becomes 'failed' and the retry_queue row is deleted. Name it so `cargo test retry_exhaustion`
    matches it. If the exhaustion logic is easier to unit-test as an extracted `fn
    exhaust_job(conn, job_id) -> rusqlite::Result<()>`, extract it and call it from both the poll loop
    and the test.

    The whole file MUST compile on Linux (toast is cfg-gated; printer stub returns Ok on Linux).
  </action>
  <verify>
    <automated>cargo test retry_exhaustion && cargo test crash_recovery && cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `src/retry_task.rs` contains `pub async fn run_retry_task(` with the exact D-03 parameter list including `printer: Box<dyn crate::printer::Printer + Send>`
    - The startup block contains the D-05 query `WHERE status='printing' AND job_id NOT IN (SELECT job_id FROM retry_queue)`
    - The poll loop uses `interval(Duration::from_secs(5))` with `MissedTickBehavior::Delay`
    - The exhaustion branch (attempt_count >= 3) contains `status='failed'`, `DELETE FROM retry_queue`, `send_health(HealthState::Problem)`, and a call to `show_print_failure_toast`
    - The success branch orders `status='printed'` BEFORE `ack_job` BEFORE `DELETE FROM retry_queue` BEFORE `send_health(HealthState::Connected)` (C4)
    - `show_print_failure_toast` is `#[cfg(windows)]`-gated with a `#[cfg(not(windows))]` stderr arm
    - `busy_timeout` is set after WAL enable
    - `cargo test retry_exhaustion` exits 0; `cargo build` succeeds on Linux
  </acceptance_criteria>
  <done>run_retry_task implements crash recovery + 5s poll + 3×/30s retry + exhaustion (toast+red tray); exhaustion unit test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Spawn the retry task in main.rs Runtime block (D-03 wiring)</name>
  <files>src/main.rs</files>
  <read_first>
    - src/main.rs (Runtime block lines 403–466 — read the full block: pusher_config build, worker_token/worker_base_url clones, proxy_for_pusher/send_health pattern, the two existing rt_handle.spawn calls, drop(print_tx))
    - src/print_worker.rs (lines 64–84 — the printer_name/printer_type read + PrinterId construction to duplicate for the retry printer)
    - .planning/phases/06-resilience/06-PATTERNS.md (main.rs section — exact retry proxy, printer construction, and spawn snippets)
    - .planning/phases/06-resilience/06-RESEARCH.md (Pitfall 4 — retry task needs its OWN Box<dyn Printer>)
  </read_first>
  <action>
    Inside the `if is_runtime { ... }` block, add the retry task wiring. All of this must occur BEFORE
    `agent_token` is moved into the pusher spawn (line ~455) — place the clones and printer
    construction alongside the existing `worker_token` / `worker_base_url` clones (around lines 424–442).

    1. Construct a SECOND `Box<dyn Printer>` for the retry task (Pitfall 4). Using the main App `conn`,
       read `printer_name` and `printer_type` via `config_store::get` exactly as print_worker.rs does
       (lines 64–84): if printer_name is missing/empty, log and SKIP spawning the retry task (do not
       hard-error — the print worker already hard-errors on missing printer, and a missing printer at
       this point means activation is incomplete). Build `PrinterId::Serial(name)` if
       printer_type=="serial" else `PrinterId::Spooler(name)`, then `let printer_for_retry =
       printer_from_entry(&retry_printer_id);`. Import `printer_from_entry` and `PrinterId` if not
       already imported at the top of main.rs.
    2. Create the retry health proxy (mirror lines 446–449): `let proxy_for_retry =
       event_loop.create_proxy(); let retry_send_health = move |state: HealthState| { let _ =
       proxy_for_retry.send_event(UserEvent::HealthChanged(state)); };`.
    3. Clone the values the task needs (before they are moved): `let retry_db_path = db_path.clone();
       let retry_http = http.clone(); let retry_token = worker_token.clone(); let retry_base_url =
       worker_base_url.clone();` (worker_token and worker_base_url are already clones in scope; cloning
       them again is fine — or clone from agent_token/auth_url before they move).
    4. After the existing print_worker spawn (line ~461) and before `drop(print_tx)`, add:
       `rt_handle.spawn(async move { crate::retry_task::run_retry_task(retry_db_path, retry_token,
       retry_base_url, retry_http, printer_for_retry, retry_send_health).await; });`.

    Do NOT change run_pusher_loop or run_print_worker calls. Do NOT reorder the existing spawns
    (order is irrelevant — all tasks are independent).
  </action>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.rs` contains `crate::retry_task::run_retry_task(` inside the `if is_runtime` block
    - A second `printer_from_entry(&retry_printer_id)` (or equivalent) constructs `printer_for_retry`
    - A `proxy_for_retry` / `retry_send_health` closure is created via `event_loop.create_proxy()`
    - The retry spawn occurs before `drop(print_tx)` and after agent_token clones are taken (no use-of-moved-value)
    - `cargo build` succeeds on the current target
    - `cargo build --target x86_64-pc-windows-msvc` is NOT required here (Linux build proves compilation; Windows toast path is cfg-gated and verified manually per VALIDATION.md)
  </acceptance_criteria>
  <done>Retry task spawns as the fourth Tokio task with its own printer + health proxy; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retry task → SQLite state.db | Local trusted store; job_ids sourced from local rows (already validated on ingest) |
| retry task → Noren (fetch_job_bytes/ack_job) | Authenticated HTTP; token via bearer only |
| retry task → Windows WinRT toast | OS notification surface; fire-and-forget, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | agent_token in retry_task logs | mitigate | token passed only into fetch_job_bytes/ack_job which use .bearer_auth; never in any eprintln! in retry_task.rs (T-02-02) |
| T-06-05 | Denial of Service | crash-recovery infinite re-fetch loop | accept | D-05 documented tradeoff (Pitfall 3): a permanently-unfetchable 'printing' row re-attempts each boot; acceptable for MVP simplicity |
| T-06-06 | Tampering | 4th concurrent WAL connection write contention on printed_jobs | mitigate | busy_timeout(5s) set after WAL enable on the retry connection (Pitfall 2); WAL serializes writers |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (all deps present since prior phases per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cargo test retry_exhaustion` — retry exhaustion unit test GREEN.
- `cargo test crash_recovery` — crash-recovery SQL invariant GREEN.
- `cargo test health_state` — Problem strings updated, distinctness preserved.
- `cargo build` — full crate compiles on Linux (retry task + main.rs wiring).
- Manual (Windows, per VALIDATION.md): mock printer that always fails → toast appears after ~90s + tray turns red.
</verification>

<success_criteria>
- Fourth Tokio task retries 3×/30s, exhausts to red tray + toast, recovers green on success.
- Boot crash recovery re-queues orphaned 'printing' rows.
- Tray Problem wording reflects printer failure.
- Full crate builds; retry_exhaustion + crash_recovery + health_state tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/06-resilience/06-03-SUMMARY.md` when done.
</output>
