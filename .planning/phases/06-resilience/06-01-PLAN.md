---
phase: 06-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/retry_task_test.rs
  - tests/pending_jobs_test.rs
  - tests/config_store_test.rs
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]
must_haves:
  truths:
    - "cargo test compiles with new test stubs present (they may fail RED — that is expected)"
    - "Every Phase 6 requirement has at least one failing test asserting its behavior"
  artifacts:
    - path: "tests/retry_task_test.rs"
      provides: "RED stubs for crash-recovery query, retry-queue insert, retry exhaustion (RES-01/02/04)"
      contains: "make_test_conn"
    - path: "tests/pending_jobs_test.rs"
      provides: "RED stubs for fetch_pending_jobs 200/error + validate_job_id call (RES-03)"
      contains: "spawn_stub"
  key_links:
    - from: "tests/config_store_test.rs"
      to: "config_store::open_and_migrate"
      via: "user_version assertion (updated to 2 in Wave 1)"
      pattern: "user_version"
---

<objective>
Wave 0 test scaffolds for Phase 6. Create failing (RED) test stubs that assert the
observable behaviors of every Phase 6 requirement BEFORE any production code exists, so
subsequent waves have a Nyquist sampling target. This plan also updates the existing
`config_store_test.rs` `user_version` assertion so it stays accurate once migration v2
lands in Wave 1.

Purpose: Establish the automated verification surface. Every downstream task in waves 1–3
has a concrete `cargo test <name>` command to prove completion.
Output: `tests/retry_task_test.rs` (new), `tests/pending_jobs_test.rs` (new), and an
updated `tests/config_store_test.rs`.

IMPORTANT — these tests are intentionally RED at the end of this plan. The functions they
call (`fetch_pending_jobs`, `run_retry_task` helpers) do not exist yet. Wave 1–3 turn them
GREEN. Do NOT implement production code in this plan. Where a test must reference a
not-yet-existing symbol, gate that specific test behind a `#[ignore = "Wave N: <reason>"]`
attribute so the suite still COMPILES; the un-ignored tests assert pure-SQL / pure-parse
behavior that can be written against an in-memory schema now.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-resilience/06-CONTEXT.md
@.planning/phases/06-resilience/06-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

Noren pending-jobs contract (VERIFIED from ~/repos/brevly/noren/src/routes/api/agent/jobs/pending/+server.ts):
Response body is a WRAPPER OBJECT, not a bare array:
```json
{ "jobs": [ { "jobId": "abc", "type": "pedido" }, { "jobId": "def", "type": "despacho" } ] }
```
Each element uses camelCase key `jobId` and key `type` (NOT `job_type`).
Auth: Bearer agentToken. 403 on invalid/missing token.

Existing HTTP stub pattern (from tests/noren_client_test.rs and tests/print_worker_test.rs):
```rust
async fn spawn_stub(status: u16, body: &'static str) -> String { /* binds 127.0.0.1:0, one-shot */ }
```
For dynamic JSON bodies, promote with `Box::leak(format!(...).into_boxed_str())` to get `&'static str`.

Existing in-memory SQLite test pattern (from src/pusher/client.rs tests, per 06-PATTERNS.md):
`rusqlite::Connection::open_in_memory()` + `conn.execute_batch("CREATE TABLE ...")`.

schema-v2 printed_jobs (target CHECK — from 06-CONTEXT.md D-01):
status TEXT CHECK(status IN ('pending','printing','printed','failed'))
retry_queue columns: job_id TEXT PK, job_type TEXT, escpos_bytes BLOB, attempt_count INTEGER,
next_retry_at TEXT, last_error TEXT, created_at TEXT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/retry_task_test.rs with RED stubs for RES-01/02/04</name>
  <files>tests/retry_task_test.rs</files>
  <read_first>
    - tests/config_store_test.rs (in-memory / tempdir SQLite test conventions)
    - src/pusher/client.rs (the `#[cfg(test)] mod tests` block ~lines 427–533: `make_test_conn` + in-memory schema pattern to copy)
    - .planning/phases/06-resilience/06-CONTEXT.md (D-05 crash-recovery query; D-06 poll query + attempt branching; D-12 retry_queue insert)
    - .planning/phases/06-resilience/06-VALIDATION.md (Per-Task Verification Map: exact test names `crash_recovery`, `crash_recovery_already_queued`, `retry_queue_insert`, `retry_exhaustion`, `retry_task`)
  </read_first>
  <action>
    Create tests/retry_task_test.rs. Add a local `make_test_conn()` helper that opens
    `rusqlite::Connection::open_in_memory()` and `execute_batch` creates two tables with the
    schema-v2 shape: `printed_jobs` (columns job_id TEXT PK, job_type TEXT, status TEXT DEFAULT
    'pending', attempt INTEGER DEFAULT 0, received_at TEXT, printed_at TEXT, failed_at TEXT — no
    CHECK needed in the test table so arbitrary statuses can be seeded) and `retry_queue` (columns
    job_id TEXT PK, job_type TEXT, escpos_bytes BLOB, attempt_count INTEGER DEFAULT 0, next_retry_at
    TEXT, last_error TEXT, created_at TEXT).

    Write these tests as pure-SQL assertions against the seeded in-memory DB (these can be GREEN or
    RED now — they encode the invariants Wave 1/2 must satisfy):

    1. `fn crash_recovery_selects_orphaned_printing_rows()`: seed printed_jobs with one row
       status='printing' whose job_id is NOT in retry_queue, and one row status='printing' whose
       job_id IS in retry_queue. Run the exact D-05 query `SELECT job_id, job_type FROM printed_jobs
       WHERE status='printing' AND job_id NOT IN (SELECT job_id FROM retry_queue)`. Assert exactly
       one row returned and it is the orphan (not the already-queued one).
    2. `fn retry_poll_selects_due_rows_oldest_first()`: seed retry_queue with three rows —
       next_retry_at in the past (two of them, different timestamps) and one in the future. Run the
       D-06 query `SELECT job_id, job_type, escpos_bytes, attempt_count FROM retry_queue WHERE
       next_retry_at <= datetime('now') ORDER BY next_retry_at ASC LIMIT 10`. Assert exactly two
       rows, ordered oldest-first.
    3. `fn retry_queue_insert_stores_blob_bytes()`: run the D-12 `INSERT OR IGNORE INTO retry_queue
       (... escpos_bytes ...) VALUES (?1,?2,?3,1,datetime('now','+30 seconds'),?4,datetime('now'))`
       binding `bytes.as_slice()` for a `Vec<u8>` of `[0x1b,0x40,0x41]`. Read back with
       `row.get::<_, Vec<u8>>(col)` and assert the bytes round-trip. Insert the same job_id twice and
       assert the second INSERT OR IGNORE leaves attempt_count unchanged (dedup on failure re-entry).
    4. `#[ignore = "Wave 2: run_retry_task not yet implemented"] fn retry_exhaustion_marks_failed()`:
       stub asserting the exhaustion invariant — seed retry_queue with attempt_count=3, document (in
       a comment) that after a failed print at attempt_count>=3 the row must be DELETEd from
       retry_queue and printed_jobs.status set to 'failed'. Keep the body as `todo!()` or an
       ignored assertion so the suite compiles.
    5. `#[ignore = "Wave 2: run_retry_task not yet implemented"] fn retry_task_smoke()`: ignored
       placeholder referencing the eventual `brevly_print::retry_task::run_retry_task` entry point so
       the module name is documented.

    Use test names that make `cargo test crash_recovery`, `cargo test retry_queue_insert`,
    `cargo test retry_exhaustion`, and `cargo test retry_task` each match at least one test.
  </action>
  <verify>
    <automated>cargo test --test retry_task_test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/retry_task_test.rs` exists and contains `fn make_test_conn`
    - `cargo test --test retry_task_test` COMPILES (exit 0 for the un-ignored SQL tests; ignored stubs are skipped)
    - `cargo test crash_recovery` matches `crash_recovery_selects_orphaned_printing_rows`
    - `cargo test retry_queue_insert` matches `retry_queue_insert_stores_blob_bytes`
    - `cargo test retry_exhaustion` and `cargo test retry_task` each match at least one (ignored) test
    - The D-05 orphan query test asserts exactly one row (the orphan, not the already-queued row)
  </acceptance_criteria>
  <done>retry_task_test.rs compiles; SQL-invariant tests pass; Wave-2 placeholders are #[ignore]d.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/pending_jobs_test.rs with RED stubs for RES-03</name>
  <files>tests/pending_jobs_test.rs</files>
  <read_first>
    - tests/noren_client_test.rs (the `spawn_stub` one-shot HTTP listener helper to copy verbatim)
    - tests/print_worker_test.rs (Box::leak trick for dynamic JSON `&'static str` bodies)
    - src/noren_client.rs (fetch_job_bytes / ack_job pattern; validate_job_id private fn ~lines 168–176)
    - .planning/phases/06-resilience/06-CONTEXT.md (D-08 pending pull, D-09 field-name contract)
    - VERIFIED Noren contract: response is `{ "jobs": [ { "jobId": "...", "type": "..." } ] }`
  </read_first>
  <action>
    Create tests/pending_jobs_test.rs. Copy the `spawn_stub(status, body)` one-shot HTTP listener
    helper from tests/noren_client_test.rs verbatim.

    Write these tests targeting the not-yet-existing `brevly_print::noren_client::fetch_pending_jobs`.
    Gate every test that calls `fetch_pending_jobs` behind `#[ignore = "Wave 3: fetch_pending_jobs
    not yet implemented"]` so the file COMPILES today (the symbol does not exist yet — reference it
    in a commented-out `use` or a fully-qualified path inside an ignored test body, whichever keeps
    `cargo test --test pending_jobs_test` compiling; prefer keeping the tests written against the
    real path and letting them be `#[ignore]`d rather than commented out, so Wave 3 just removes the
    ignore attribute).

    Tests:
    1. `#[tokio::test] #[ignore = "Wave 3"] async fn fetch_pending_jobs_200_parses_wrapper()`: stub
       returns `{"jobs":[{"jobId":"job-1","type":"pedido"},{"jobId":"job-2","type":"despacho"}]}`.
       Assert `fetch_pending_jobs(...)` returns a Vec of length 2 whose first element has
       job_id=="job-1" and job_type=="pedido". This test encodes the D-09 contract: the response is a
       WRAPPER object with a `jobs` array, each element keyed `jobId`/`type`.
    2. `#[tokio::test] #[ignore = "Wave 3"] async fn fetch_pending_jobs_empty_array()`: stub returns
       `{"jobs":[]}`. Assert an empty Vec is returned (Ok, not Err).
    3. `#[tokio::test] #[ignore = "Wave 3"] async fn fetch_pending_jobs_non_200_returns_err()`: stub
       returns 500. Assert `fetch_pending_jobs(...)` is Err.
    4. `#[test] fn validate_job_id_rejects_traversal_documented()`: a NON-ignored pure test that does
       NOT call fetch_pending_jobs. Since `validate_job_id` will be made `pub(crate)` (not
       `pub`), it cannot be called from an integration test. Instead assert the SECURITY invariant at
       the string level: build the set of forbidden chars `['/','.','\\','?','#','%','\0']` and assert
       a candidate id like `"../admin"` contains at least one of them (documents CR-02 expectation that
       Wave 3 wiring must reject such ids before insert_print_job). Add a comment: "Wave 3 pusher
       wiring MUST call validate_job_id on every job.job_id from fetch_pending_jobs (CR-02)."

    Name tests so `cargo test fetch_pending_jobs` matches tests 1–3.
  </action>
  <verify>
    <automated>cargo test --test pending_jobs_test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/pending_jobs_test.rs` exists and contains `spawn_stub`
    - `cargo test --test pending_jobs_test` COMPILES and exits 0 (Wave-3 tests #[ignore]d, doc test passes)
    - `cargo test fetch_pending_jobs` matches at least three tests (all currently ignored)
    - Test 1 body asserts the wrapper shape `{"jobs":[...]}` with `jobId`/`type` keys — NOT a bare array
    - A non-ignored test documents the CR-02 validate_job_id expectation
  </acceptance_criteria>
  <done>pending_jobs_test.rs compiles; Wave-3 HTTP tests are #[ignore]d; CR-02 doc test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update config_store_test.rs user_version assertions for migration v2</name>
  <files>tests/config_store_test.rs</files>
  <read_first>
    - tests/config_store_test.rs (current assertions `assert_eq!(user_version, 1, ...)` at lines ~18, 45, 53)
    - src/config_store.rs (MIGRATIONS vec — currently a single v1 M::up; Wave 1 adds v2)
    - .planning/phases/06-resilience/06-RESEARCH.md (Pitfall 7: user_version advances 1→2 after v2)
  </read_first>
  <action>
    This test MUST stay RED until Wave 1 adds migration v2 — that is intentional and correct
    Nyquist behavior (the assertion pins the invariant that Wave 1 satisfies).

    In `test_schema_v1_and_user_version`: change the assertion from `assert_eq!(user_version, 1, ...)`
    to `assert_eq!(user_version, 2, "user_version must be 2 after migration v2 (Phase 6)")`. Rename
    the function to `test_schema_and_user_version` (drop the `_v1` suffix). Keep the three-table
    existence loop (`config`, `printed_jobs`, `retry_queue`) unchanged — all three still exist after
    v2 (printed_jobs is recreated, not dropped permanently).

    In `test_open_and_migrate_idempotent`: change BOTH `assert_eq!(v1, 1)` and `assert_eq!(v2, 1, ...)`
    to expect `2`, and update the message strings to reference v2. The idempotency invariant is
    unchanged: re-opening an already-migrated (now v2) DB is a no-op that leaves user_version at 2.

    Do NOT touch `test_write_read` or `test_get_absent_key`.

    Add a `#[test] fn printed_jobs_accepts_printing_status()`: open_and_migrate a tempdir db, then
    `conn.execute("INSERT INTO printed_jobs (job_id, status) VALUES ('j1','printing')", [])` and assert
    it succeeds (Ok) — proving the v2 CHECK constraint admits the new 'printing' value. This test is
    also RED until Wave 1 (the v1 CHECK rejects 'printing').
  </action>
  <verify>
    <automated>cargo test --test config_store_test 2>&1 | grep -q "test result" && echo "compiles+runs"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/config_store_test.rs` asserts `user_version == 2` in both migration tests
    - A new test `printed_jobs_accepts_printing_status` inserts status='printing' and asserts Ok
    - The file COMPILES (`cargo test --test config_store_test` runs; the user_version + printing tests are RED until Wave 1 — expected)
    - `test_write_read` and `test_get_absent_key` are unchanged
    - grep confirms no remaining `assert_eq!(user_version, 1` or `assert_eq!(v1, 1` / `assert_eq!(v2, 1` literals
  </acceptance_criteria>
  <done>config_store_test.rs expects user_version=2 and 'printing' status; RED until Wave 1 migration lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → in-memory SQLite | Test-only data; no untrusted input |
| test HTTP stub → fetch_pending_jobs | Simulates the Noren network boundary; encodes CR-02 job_id validation expectation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | job_id from pending pull | mitigate | Task 2 encodes a documented CR-02 invariant test asserting traversal chars are recognized; Wave 3 enforces the real `validate_job_id` call |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs zero new packages (RESEARCH Package Legitimacy Audit: all deps present since prior phases). No install task in this plan. |
</threat_model>

<verification>
Run `cargo test --test retry_task_test --test pending_jobs_test --test config_store_test`.
Expected: retry_task_test and pending_jobs_test COMPILE and their non-ignored tests PASS;
config_store_test COMPILES but its user_version / printing tests are RED (they go GREEN in Wave 1).
No production `src/` files are modified in this plan.
</verification>

<success_criteria>
- Two new test files compile and their non-ignored assertions pass.
- Every Phase 6 requirement (RES-01/02/03/04) has at least one test (ignored or active) referencing it.
- config_store_test.rs pins user_version=2 and 'printing' acceptance (RED until Wave 1).
- `cargo build` still succeeds (no src changes).
</success_criteria>

<output>
Create `.planning/phases/06-resilience/06-01-SUMMARY.md` when done.
</output>
